# 13画面の棚卸し（見本データ → 本物のデータ）

最終更新：2026-08-26
Preview：https://gacha-os-l44xrji4s-haruharu1100s-projects.vercel.app

> この表は「終わったこと」を書き換えていく生きた表です。
> ★終わっていない画面の行を、先に○へ書き換えないこと。
>   書き換えた瞬間に、この表は「作業の予定表」に変わり、
>   受け取った方には、どこまで本当に動くのか分からなくなります。

---

## この表が、なぜ先に要るのか

「固定の数字を、DBの数字に置き換える」だけでは終わりません。
置き換えたあとに、**作る・直す・消す・絞り込む・詳しく見る・記録に残す・権限で止める**
までできて、はじめて「本物になった」と言えます。

だから先に、画面ごとに

- いま、どこから数字が来ているのか
- 本当は、どの表（DB）から来るべきなのか
- まだ無い入口（API）はどれか

を、1画面ずつ確かめて書きます。**推測では書きません。**
実際にコードを読んで確かめた結果だけを書いています。

---

## いちばん大事な発見

13画面のうち **8画面** は、DBを一度も見ていません。

見ているのは `lib/console/state.ts` の `initialState()`（3,364行）という、
**ブラウザの中だけに置かれた見本データ**です。

これが意味することは2つあります。

1. **画面を再読み込みすると、操作した内容が消えます。**
   サーバーに保存していないからです。
2. **会社ごとの分離（テナント分離）が、この8画面では効きようがありません。**
   そもそもDBを見ていないので、「どの会社のデータか」という考えが無いためです。

サーバー側のテナント分離は既に実測で合格していますが、
それは**DBを見ている画面についてだけ**の話です。ここを取り違えないこと。

---

## Aランク（運営の方が毎日見る）8画面

| # | 画面 | ファイル | いまの数字の出どころ | 本来の正本（DB） | 入口（API） | 状態 |
|---|---|---|---|---|---|---|
| 1 | ダッシュボード | `Dashboard.tsx` | **半分だけ本物** | 下記参照 | 一部あり | △ |
| 2 | ガチャ管理 | `GachaList.tsx` | **本物（DB）** | `gachas` / `gacha_stock` / `prizes` / `draws` | `/api/console/gachas` | ○ |
| 3 | 還元率 | `RtpScreen.tsx` | **本物（DB）** | `gachas` / `draws` / `prizes` | `/api/console/rtp` | ○ |
| 4 | 会員管理 | `CustomersScreen.tsx` | **本物（DB）** | `customers` / `point_ledger` / `draws` / `prizes` / `orders` / `shipments` / `support_tickets` / `fraud_flags` / `login_attempts` | `/api/console/customers`<br>`/api/console/customers/action` | ○ |
| 5 | ポイント管理 | `PointScreen.tsx` | 見本 `DEMO_LEDGER` | `point_ledger` / `point_adjustments` | 一部（`/api/console/points`） | ✗ |
| 6 | 注文管理 | `OrdersScreen.tsx` | **本物（DB）** | `orders` / `order_items` | `/api/console/orders` | ○ |
| 7 | 発送管理 | `ShipmentsScreen.tsx` | **本物（DB）** | `shipments` / `shipment_items` | `/api/console/shipments` | ○ |
| 8 | 問い合わせ | `SupportScreen.tsx` | 見本 `DEMO_TICKETS` | `support_tickets` | **無い** | ✗ |

### 1. ダッシュボードの内訳（ここが特に紛らわしい）

同じ画面の中で、本物と見本が**並んで**表示されています。
運営の方には見分けがつきません。ここが一番危ない状態です。

| 出ている数字 | いまの出どころ | 判定 |
|---|---|---|
| 未発送の件数 | `/api/console/summary`（DB） | ○ 本物 |
| 注文の件数 | `/api/console/summary`（DB） | ○ 本物 |
| 本日の売上 | `summary(s)` ＝ 見本 | ✗ |
| 今月の売上 | `summary(s)` ＝ 見本 | ✗ |
| 今月の粗利益 | `s.gachas` の合計 ＝ 見本 | ✗ |
| 本日のプレイ数 | `summary(s)` ＝ 見本 | ✗ |
| 会員数 | `summary(s)` ＝ 見本 | ✗ |
| 公開中のガチャ | `summary(s)` ＝ 見本 | ✗ |
| 今日やること（注文・発送の分） | `liveTodos(live)`（DB） | ○ 本物 |
| 今日やること（それ以外） | `todayTodos(s)` ＝ 見本 | ✗ |

---

## Bランク（管理・安全）5画面

| # | 画面 | ファイル | いまの数字の出どころ | 本来の正本（DB） | 入口（API） | 状態 |
|---|---|---|---|---|---|---|
| 9 | Fraud Center | `FraudCenter.tsx` | 見本 `DEMO_SIGNUPS` | `fraud_flags` / `login_attempts` | **無い** | ✗ |
| 10 | Analytics | `AnalyticsScreen.tsx` | **日付ごと固定**（下記） | `draws` / `orders` / `point_ledger` | **無い** | ✗ |
| 11 | Security Center | `SecurityCenter.tsx` | **一部だけ本物** | 下記参照 | 一部あり | △ |
| 12 | 相場管理 | `MarketScreen.tsx` | 固定表 `lib/console/market.ts` の `MARKET_ROWS` | `market_prices` | **無い** | ✗ |
| 13 | Settings | `SettingsScreen.tsx` | 見本＋**保存されない** | `settings` | **無い** | ✗ |

### 10. Analytics — 日付が止まっている

グラフの中身は、コードに直接書かれた7日分です。

```
8/16 / 8/17 / 8/18 / 8/19 / 8/20 / 8/21 / 8/22
```

**時間がたつほど危なくなる種類の固定値です。**
日付が動かないので、いつか「1年前の週の売上」を
今週の売上として見せることになります。

※ 画面の先頭で「未接続」と断ってはあります（それ自体は正しい作りです）。
　 ただし断り書きは、実データ化の代わりにはなりません。

### 11. Security Center の内訳

| 出ている項目 | いまの出どころ | 判定 |
|---|---|---|
| 監査ログの改ざん検知 | `/api/console/audit/verify`（DB） | ○ 本物 |
| それ以外の安全10項目 | 見本 | ✗ |

### 13. Settings — 保存できない入力欄がある

画面に、こう書いてあります。

> このデモでは、設定を変えても保存されません。ページを閉じると元に戻ります。

正直に書いてある点は良いのですが、
**保存できない入力欄は、そもそも置かない**というのがご指示です（項目22）。
`settings` テーブルは既にあるので、つなぎ先はあります。

---

## まとめ（数で見る）

| 状態 | 画面数 | 画面 |
|---|---|---|
| ○ 本物のデータ | 6 | 還元率・注文管理・発送管理・ダッシュボード・**ガチャ管理**・**会員管理** |
| △ 半分だけ本物 | 1 | Security Center |
| ✗ 全部が見本 | 6 | ポイント管理・問い合わせ・Fraud・Analytics・相場・Settings |

★この表が6:1:6になっても、**PRODUCT PASS = NO のままです。**
　見本の画面が1つでも残っているあいだは、運営の方には
　どの画面の数字を信じてよいか分かりません。

---

## 作らなければならない入口（API）

DBの表はもう在ります。足りていないのは、画面とDBをつなぐ入口です。

| 入口 | どの画面のため | 使う表 |
|---|---|---|
| ~~`/api/console/gachas`~~ **作成済み** | ガチャ管理 | `gachas` `gacha_stock` `prizes` |
| ~~`/api/console/customers`~~ **作成済み** | 会員管理 | `customers` `point_ledger` `orders` `prizes` |
| `/api/console/support` | 問い合わせ | `support_tickets` |
| `/api/console/fraud` | Fraud Center | `fraud_flags` `login_attempts` |
| `/api/console/analytics` | Analytics | `draws` `orders` `point_ledger` |
| `/api/console/market` | 相場管理 | `market_prices` |
| `/api/console/settings` | Settings | `settings` |
| `/api/console/summary` の拡張 | ダッシュボード | 売上・プレイ数・会員数・公開中 |
| `/api/console/security` | Security Center | 各種 |

---

## 置き換えのときに、必ず一緒にやること

固定値をDBに差し替えるだけでは、合格にしません（ご指示 項目3）。
1画面ごとに、次の7つが揃って完成とします。

1. 一覧が出る（絞り込みができる）
2. 詳しく見られる
3. 作れる・直せる・消せる（その画面に必要な範囲で）
4. 会社をまたいで見えない（A→B・B→A の両方向で実測）
5. 権限で止まる（画面だけでなく**入口＝APIでも**止まる）
6. 大事な変更が監査ログに残る（理由つき）
7. 0件のときに、架空の数字を出さない（Empty State）

---

## いま出してよい判定

```
PRODUCT PASS = NO
```

理由：契約者が毎日見る画面のうち、8画面がDBを見ていないためです。
この状態では「管理サイトに嘘の数字が1個も無い」と言えません。

外部サービス（メール・決済・配送会社）は別判定です。
つないでいないこと自体は、この13画面の合否には関係させません（ご指示 項目37）。
