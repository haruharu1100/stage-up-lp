# ご確認のお願い（実績還元率／担当者の管理）

提出：2026-08-26
対象：AI GACHA OS 管理サイト

---

## この紙は何か

ご指示の **項目37** にある14点を、順番どおりに並べたものです。

★ここで一度止めます（項目38）。
　見本データ13画面の差し替えには、まだ着手していません。
　メール・決済・配送会社にも、手をつけていません。

★このあと **ご指示3点** をいただいたので、そのぶんも行いました。
　最後の「★ご指示3点の結果」にまとめてあります。

---

## ① 公開先（Preview URL）

    https://gacha-os-42k2mr4ec-haruharu1100s-projects.vercel.app

* 本番（Production）には出していません。Preview のみです。
* 会社コード `DEMO` ／ メール `boss@demo.example`
* データはすべて架空です。実在のお客様の情報は入っていません。
* ★このURLには、LPの書き換え（後述）も入っています。

---

## ② 実績還元率の画面

`docs/shots/admins/01-rtp.png`

実際に出ている数字（お試しガチャ）：

| | |
|---|---|
| 設計還元率 | 88.0％ |
| 残数還元率 | 23.3％ |
| **実績還元率** | **51.0％** |
| サンプル数 | 1,135回 |
| 判定 | 危険 |

画面のいちばん上に、こう出ます。

    ガチャ 3本 ／ 危険 1本 ／ 注意 0本 ／ まだ判断できない 2本

★「まだ判断できない 2本」が、いちばん大事なところです。
　売れた回数が足りないガチャに、0％や100％を出していません。
　「売れた回数が足りないものです」と、理由を書いて逃げずに止めています（項目9・13）。

---

## ③ ガチャ一覧

`docs/shots/admins/02-gacha-list.png`

表の上に、先に呼び分けを書きました。

    還元率は、3種類あります。
    設計（作ったときの予定）・残数（残っている景品 ÷ 残っている口数）・
    実績（実際にお客様へ返った額）。
    この一覧に出ているのは 設計 と 残数 です。
    実際にいくら返ったかは、実績還元率 の画面でご確認ください。

列も「設計還元率」「残数還元率」に直しました。

★**ここは、まだ正直に「見本」と断っています。**
　一覧の数字そのものは、まだ架空です。
　画面の下に「すべて架空の見本です」と出しています。
　3種類を **数字で** 一覧に並べる（項目4の後半）のは、
　この一覧を本物のデータに差し替えたあとになります。
　その差し替えは、13画面のうちの2番目です＝**項目38の停止線の向こう側**です。

---

## ④ 実績還元率の計算根拠（項目6・7）

計算は **すべてサーバー側**です。画面では一切計算していません。

分母と分子を、そのまま画面に出しています。

| | 中身 |
|---|---|
| 分母 | 実際に売れた金額（抽選記録の合計） |
| 分子 | お客様に返った価値。上位賞は景品の価値、それ以外は戻したポイント |

---

## ⑤ 88％→18％ の再発防止（項目11・12）

事故の中身は、**％と比率の取り違え**でした。

    データ側が書いた値   0.88
    計算側が待っていた値  88

人の目には、どちらも「88パーセント」に見えます。
機械は 0.88％ として読みました。だから 18.23％ になりました。

この1件を、**永久に固定する専用の試験**にしました。

* `0.88` と `88` で結果が変わらないことを確かめる
* 境界値 `0 / 0.01 / 0.88 / 1 / 18.23 / 88 / 100 / 120` を全部通す

試験の結果：**531本すべて合格**（`npm test`）。

---

## ⑥ 担当者の一覧

`docs/shots/admins/03-admins-list.png`

本物のデータです（7名・全員 `@demo.example`）。
お名前／メール／権限／状態／2段階認証／最終ログイン を出しています。

ご自身の行には、こう書いてあります。

    ご自身の権限を下げることも、ご自身を止めることもできません。
    最後の管理者（全権）も、降格・停止はできません。

★押せないだけでなく、**押せてしまっても断ります**（下の⑪をご覧ください）。

---

## ⑦ 権限を変える

`04-role-pick.png` → `05-role-warn.png` → `06-role-reason.png` → `07-role-code.png`

4段階です。

1. 新しい権限を選ぶ（**いまと同じ権限は、選べないように潰してあります**）
2. できることが増えるときは、はっきり警告する
3. 理由を書く（必須・4文字以上・記録に残ります）
4. 直前にもう一度、認証アプリの6桁を入れ直す

★4 が大事です。ログイン時の6桁ではなく、**押す直前の**6桁です。
　席を離れている間に、開いたままの画面から実行されるのを防ぎます。

---

## ⑧ 利用を止める

`08-suspend-reason.png` → `09-suspend-code.png` → `10-suspend-ng.png` → `11-suspend-last.png`

理由 → 6桁 → 最終確認、の順です。
止める前に「この方が対象です」とお名前・メール・権限を出しています。

★`10-suspend-ng.png` は、**わざと出鱈目な6桁を入れた**ところです。

    6桁の数字が正しくありません。

と、はっきり断られて、そこから先へ進めません。

★`11-suspend-last.png` が、**その先の最終確認**です。
　正しい6桁を入れると、ここまで来ます。押す前に、こう出ます。

    実行すると、次のことが起こります
      ・この方は、いますぐログインできなくなります
      ・開いたままの画面も、その場で使えなくなります
      ・理由と実行者が、監査ログに残ります
      ・元に戻すには、もう一度6桁が要ります

　理由と「あなた（いま6桁で確認済み）」も、同じ画面に出しています。

---

## ⑨ ログインが、その場で切れること

公開先で実測しました。

| 確かめたこと | 結果 |
|---|---|
| 止めると、その人のログインは、その場で全部切れる | 401・残りログイン **0件** |
| 止めた人は、正しい合言葉でもログインできない | 403 SUSPENDED |
| 停止を解除すると、また入れる（権限は変わらない） | ACTIVE に復帰 |
| **権限を変えても、その人のログインは切らない** | 作業中に落としません |
| 権限を変えると、開いたままの画面が、その場で断られる | 200 → 同じ画面のまま 403 |

★権限変更と利用停止で、わざと扱いを変えています。
　権限を変えただけで作業中の方を叩き出すのは、乱暴です。
　止めるときは、迷わず全部切ります。

---

## ⑩ 監査ログ

`docs/shots/admins/12-audit.png`

権限変更・停止・解除が、**すべて理由つき・実行者名つき**で残ります。

    ROLE_CHANGE：2件　USER_SUSPEND：4件（全部に理由と実行者あり）

★**断られた操作は、成功として記録されません。**
　3回断られても、記録は6件のままでした。

---

## ⑪ 会社の分離（テナント分離）

| 確かめたこと | 結果 |
|---|---|
| 他社の担当者は、一覧に1人も出ない | 7名全員が自社 |
| 他社の担当者は、IDを知っていても変えられない | 404 |
| そのとき、他社の存在すら教えない | メッセージに「他社」と書かない |

★404 にしているのは、わざとです。
　「権限がありません」と返すと、**その ID が実在することを教えてしまいます。**

---

## ⑫ 通しの試験（E2E）

### 手元（Unit / Local）

    531本すべて合格

### 公開先の実測（Preview）

    105 / 105 通りました

報告書：`docs/点検_公開環境.md`

このうち **22件が、今回追加した担当者管理の試験**です。
すべて「画面ではなく、入口（API）を直接叩いて断られるか」を見ています（項目27）。

いちばん効いた3つ：

| | |
|---|---|
| ★知らない役割の名前は断る | `GOD_MODE` を送ると 400。**黙って弱い権限に丸めません** |
| ★止めると、その場で全部切れる | 残りログイン0件を、DBを数えて確認 |
| ★停止中の管理者を「最後の1人」に数えない | 停止中は数えず1名 → 解除で2名に戻る |

---

## ⑬ まだ見本データのままの13画面

一覧は `docs/提出_見本データ13画面.md` にまとめました。

19画面のうち、**本物が6画面／見本が13画面**です。

本物の6画面：ダッシュボード／注文／発送／監査ログ／AIオペレーター／**実績還元率**

直す順番（毎日いちばん見る順）：

    1 ダッシュボード（済）→ 2 ガチャ一覧 → 3 還元率（済）→ 4 会員 →
    5 発送（済）→ 6 問い合わせ → 7 ポイント → 8 注文（済）→
    9 不正検知 → 10 集計 → 11 セキュリティ → 12 相場 → 13 設定

★13画面のうち、**新しくDBを作る必要があるものは1つもありません。**
　足りないのは、すでにあるデータを画面へ渡す入口（API）だけです。

★いちばん危ない見本は **不正検知** です。
　画面の中で `0.944` という数字を掛けているだけで、**判定していません。**

---

## ⑭ 判定

### PRODUCT PASS = **NO**（据え置き）

| | 条件 | いま |
|---|---|---|
| 1 | 主要な管理画面が、実データで動く | **✗ 6／19** |
| 2 | 実績還元率を確認できる | ✓ |
| 3 | 権限を変更できる | ✓ |
| 4 | 利用停止できる | ✓ |
| 5 | 変更が保存される | ✓ |
| 6 | 会社ごとに分離されている | ✓ |
| 7 | お客様側と連動している | △ 一部 |
| 8 | LPで「対応」と書いた機能が、実際に動く | △ 外部連携が未接続 |

**2〜6は、今回すべて ✓ になりました。**
残っているのは 1 です。これがいちばん大きい残りです。

---

# ★ご指示3点の結果

## 1つ目：Previewで、最後まで押して試していただけるようにしました

これまで `10-suspend-ng.png` のところで止まっていました。

    6桁の数字が正しくありません。

原因は、**Previewの担当者が、認証アプリを登録していなかった**ためです。
不具合ではなく、入口が正しく閉じている状態でした。

ご指示をいただいたので、`boss@demo.example` に登録しました。

    TOTP enrolled: YES

### ★お詫びと、鍵の作り直しについて

はじめ、私はこの紙に**鍵そのものを書いてしまいました**。これは誤りでした。

二段階認証は「その人しか知らない」ことだけが根拠です。
いちど人の目に触れた鍵は、Preview のものであっても、その根拠を失います。

ご指摘をいただき、**その鍵は捨てて、新しい鍵に作り直しました。**

| | |
|---|---|
| 前の鍵 | **無効にしました**（もう使えません。実際に叩いて確かめてあります） |
| 新しい鍵 | 登録済み |
| 開いたままのログイン | **切っていません**（作業中に落とさないため） |
| 押す直前の6桁の確認 | **全部取り消しました**（前の鍵で通した確認を残さないため） |
| 監査ログ | `MFA_ROTATED` として、理由つきで記録 |

### お手元の認証アプリへの登録のしかた

鍵は、**もう画面にも、この紙にも出しません。**
お手元のこのファイルを開いて、登録してください。

    gacha-os-lp/.secrets/mfa-preview.txt

* このファイルは git に入りません（`.gitignore` 済み）。
* 「時間ベース（TOTP）」を選んでください。アカウント名は何でも構いません。
* Google Authenticator ／ Microsoft Authenticator ／ 1Password、どれでも動きます。
* **登録が終わったら、このファイルは削除してください。**

### 使う場面は2つです

| いつ | 何を入れるか |
|---|---|
| ログインするとき | そのとき出ている6桁 |
| 「権限を変える」「利用を止める」を押す直前 | **入れ直し**（ログイン時のものは使えません） |

★2つ目が大事です。席を離れている間に、
　開いたままの画面から実行されるのを防ぐためです。

★**この鍵は Preview 専用です。**
　本番では使いません。本番では、ご本人が画面のQRコードを読み取って登録します。
　作り直しの道具（`scripts/rotate-mfa-preview.mjs`）は、
　つなぎ先が本番に見えたら**何もせずに止まる**ようにしてあります。

★**今後、報告に書くのは `TOTP enrolled: YES` までにします。**
　鍵そのもの・鍵の一部・QRコードの文字列は、報告にもチャットにも出しません。

★登録したことで、点検の道具（105項目）も6桁が要るようになりました。
　道具の側も直してあります。**直したあと、あらためて 105／105 通っています。**

---

## 2つ目：LPの言葉を直し、実績還元率を売り文句として足しました

ご指示は「両方やる」でしたので、両方行いました。

### ① 言葉を、意味に合わせて直した（204か所）

| 直した先 | どういう場所か |
|---|---|
| **残数還元率** | 「残っている景品 ÷ 残りの販売総額」を指していた場所（大半） |
| **実績還元率** | 管理画面の「実績還元率」の画面そのものを指していた場所 |

★**わざと直さなかった箇所が6つ**あります。

    「★「実還元率」に戻さないこと」

という、**戻さないための警告文**です。
ここを書き換えると、警告の意味そのものが消えます。

★このあと、**LPを実際に画面で見て、言い方の揺れをさらに13か所見つけました。**
　「3種類を混ぜないこと」というご指示の趣旨に反していたので、こちらも直しました。

| 直す前 | 直したあと | 直した理由 |
|---|---|---|
| 設定時還元率 | 設計還元率 | 同じものを2通りの名前で呼んでいた |
| 設定還元率 | 設計還元率 | 同上 |
| 残数ベース還元率 | 残数還元率 | 同上 |
| 残数ベース | 残数還元率 | 「ベース」だけでは何の割合か分からない |

★さらに、**言葉が行の途中で割れないための守りの一覧**にも、3つの正式名称を
　そろえて登録し直しました。「実績還元率」が抜けていたためです。
　（狭い画面で「実績還」／「元率」と割れるのを防ぎます）

### ② 「実績還元率」を、新しい売り文句としてLPに足した

新しい幕を1つ足しました。

    設計値は、約束にすぎません。
    実際にいくら返したかまで見張ります。

中身は3つです。

| | |
|---|---|
| 1 | 設計・残数・実績を、**横に並べて言い切る**（いつ見る数字なのかも書く） |
| 2 | **分母と分子を、必ず一緒に出す**（割合だけでは、正しいか確かめられない） |
| 3 | **分からないときに 0％や100％を出さない**（「まだ判断できません」と理由を出す） |

★置き場所は「販売中に、システムが何を見張っているのか」の**いちばん先頭**です。
　これから の数字（設計・残数）の話より **前** に置きました。
　「予定の数字だけを信じないでください」を先に言うためです。

★金額の直前の要約カードも、1枚差し替えました。

    前： リアルタイム還元率 ／ 残り口数と売上が動く
    後： 還元率を3つで見張る ／ 設計・残数・実績を分けて出す

　ここに載っていないものは、お客様にとって「買うもの」に入りません。

★**書かなかったこと**（景品表示法）
　「必ず儲かる」「絶対に赤字にならない」とは書いていません。
　この機能がするのは **気づける状態をつくること** までで、
　止めるか続けるかを決めるのは運営の方です。そう書いてあります。

★見た目の検査（7つの画面幅）も、公開先で通しました。
　日本語の途中改行・はみ出し・列の潰れ、いずれも0件です。

---

## 3つ目：gitに記録しました

変更したファイルを**名指しで**追加して、記録しました。
**push（外への送信）はしていません。**

---

## いま止まっているところ

    ここで一度止めます（項目38）。

* 見本データ13画面の差し替え … **未着手**
* メール／決済／配送会社 … **未着手**
* 本番（Production）への公開 … **していません**
