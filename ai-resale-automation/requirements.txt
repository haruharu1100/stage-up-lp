# ============================================================
# AI物販自動化システム 必要ライブラリ
# ------------------------------------------------------------
# インストール方法:
#   pip install -r requirements.txt
# ============================================================

# Claude API (Anthropic公式SDK)
anthropic>=0.40.0

# .envファイルの読み込み
python-dotenv>=1.0.0

# Googleスプレッドシート連携(任意・使う場合のみ)
gspread>=6.0.0
google-auth>=2.30.0

# 表形式データの整形・出力に使用
tabulate>=0.9.0
