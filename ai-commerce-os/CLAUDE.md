# AI Commerce OS — AI向け作業ルール

## 作業前に必ず読む（順番も守る）

1. `../事業Vault/AI Commerce OS/00_設計書v1_最上位.md`
2. 同フォルダ内の全ファイル
   - 01_DB設計案 / 02_Phase0_外部連携調査 / 03_安全設計と事故防止 / 04_ロードマップ / 05_Obsidianナレッジ構造
   - 06_商品大量生成とパイプライン / 07_価格戦略設計 / 08_仕入先ネットワークと勝ち筋拡張
   - 09_Phase1実装記録 / 10_Phase2実装記録 / 11_Phase3実装記録 / 12_Phase3.5実装記録
     / 13_Phase3.6実装記録 / 14_Phase3.7実装記録
3. `../事業Vault/DIGEST.md`

矛盾したら **最新のユーザー指示 > DIGEST > 設計書フォルダ** の順で優先する。

## 仕様変更の扱い

新しい指示はその場限りにせず、**必ず `00_設計書v1_最上位.md` へ追記・統合する**。
メモを増やして仕様を分散させない。古い記述と矛盾したら古い方を書き換える。

## 絶対に崩さないルール

```
正確性 > 安全性 > 利益 > 自動化率 > 商品数
```

1. **無断で外部サイトを自動操作しない。** 公式API / 法人API / データフィード / CSV / Webhook / 規約上認められた方法のみ。
   使えないなら機能を落として `CONNECTOR_NOT_AVAILABLE` / `MANUAL_APPROVAL_REQUIRED` を返す。**スクレイピングで埋めない。**
2. **Connector の capability が `api` でなければ、自動実行を実行層が拒否する。** 環境変数で true にしても動かさない。
3. **金銭を動かす処理は必ず Action Gate を通す。** 直接 Connector を呼ぶ実装を書かない。
4. **idempotency_key に UNIQUE 制約。** 購入・出品・発送・注文取込の4つは必須。
5. **在庫は個体単位。** SKU×数量方式にしない（二重販売の原因）。
6. **外部APIの「応答不明」は再送しない。** 人間へエスカレーションする。
7. **LLMに金額計算をさせない。** 利益・ROI・スコア・資金配分は決定的なコードで計算する。LLMは説明と分類のみ。
8. **LLM出力は必ずスキーマ検証。** 落ちたら SKIP（Fail Closed）。
9. **AIは自分の閾値・予算・自動化モードを変更できない。** 提案のみ。変更は人間。
10. **APIキーをコードに書かない。** `.env` / Secret Manager のみ。
11. **既定は `COMMERCE_AUTO_MODE=SAFE` / `SANDBOX=true`。** 勝手に変えない。
12. **虚偽・誇大な商品説明を生成しない。** 景表法・古物営業法を守る。
13. **商品数を増やすために判定を甘くしない。** しきい値を下げるのではなく仕入先を増やす。
14. **重複防止3層（L1 出品重複 / L2 個体重複 / L3 商品重複）を必ず通す。** L1はDBのUNIQUE制約で担保する。
15. **LLM判定は段階的コストフィルタの最終段（L4）でのみ呼ぶ。** 想定粗利がLLM費用の20倍未満なら呼ばない。
16. **値上げは必ず上限クランプを通す。** 相場上限・`MAX_PRICE`・`MAX_STEP_UPS`・売却確率下限のいずれかに触れたら値上げしない。
17. **未定義の商品状態ラベルを推測でマッピングしない。** 共通6段階（N/S/A/B/C/D）に無ければ `MANUAL_REVIEW`。
18. **商品マスタの統合は `merged_into` で行い、削除しない。** 実行は人間のみ。

## Fail Closed の3原則

- 分からないなら買わない
- データがおかしいなら止まる
- APIがおかしいなら止まる

## 現状

**Phase 3.8 実装済み（2026-08-20）。** 範囲は 取得 → 比較 → Route生成 → 利益計算 → SHADOW記録
→ 7/30/90日の答え合わせ → 予測誤差 → Route勝率 → 実績によるスコア補正
→ 手数料の確からしさ判定 → 実市場データ判定 → 3本立て予測（RAW/CALIBRATED/CONSERVATIVE）
→ 誤りの種類分け（FP/FN） → STRONG BUY 的中率 → Phase 4 卒業条件の機械判定
→ メルカリの実出品を1件ずつ追跡（初回/24h/3日/7日/14日/30日）＋人間の作業時間の実測
→ 取引費用と振込費用の分離／支払方法ごとの手数料／費目ごとの確からしさ／検証ファネル
→ **まず10件のPILOT（落ちた理由の8分類／同一商品の答え合わせ／工程ごとの実測から
次に自動化する工程を決める／データ汚染の点検／100件へ進む安全条件7項目／①〜⑧の報告）** まで。
実購入・実出品・実決済・実発送・外部サイトへの自動ログイン・自動値上げ・無許可スクレイピングは
**コードごと存在しない**（フラグOFFではなく未実装）。
所有していない商品の先行販売もしない（`ownership_state` は全件 `NOT_OWNED`）。

- 実装記録：`09_Phase1実装記録.md` → `10_Phase2実装記録.md` → `11_Phase3実装記録.md`
  → `12_Phase3.5実装記録.md` → `13_Phase3.6実装記録.md` → `14_Phase3.7実装記録.md`
  → `15_Phase3.8実装記録.md`（`../事業Vault/AI Commerce OS/`）
- 受け入れテスト：`npm run test:phase1`（66項目）／`npm run test:phase2`（50項目）／
  `npm run test:phase3`（113項目）／`npm run test:phase3-5`（112項目）／
  `npm run test:phase3-6`（107項目）／`npm run test:phase3-7`（122項目）／
  `npm run test:phase3-8`（128項目）。**合計698項目。**
  **仕様を変えたら7つとも通す。1つだけ通して満足しない。あわせて `npm run build` も通す**
  （画面のバンドルエラーはテストでは見つからない）。
- 費用の計算を変えたら `npm run fees`（手数料の版と状態の再計算）→ `npm run routes`（Route再生成）
  を順に流す。流さないと、古い費用で作られたRouteが残って受け入れテストが落ちる。
- 日次レポート：`npm run report`（結論1行＋卒業条件の内訳）。画面は `/readiness`。
- 実市場での答え合わせ：`npm run validation`。画面は `/validation`。
- 市場ごとの取得可否の確認：`npm run observe`（`--venue=X` で1市場ずつ）。
- Phase 4 へ進む前に `04_ロードマップ.md` の卒業条件を確認する。
  **2026-08-20 時点で卒業条件は 0 / 14 項目**
  （実市場の出品 **0 / 100件**、手数料の実額確認 **1 / 18件**、STRONG BUY 的中率 **測定不能**、作業時間 **未計測**）。
- **100件の前に、まず10件のPILOTを通す。** 画面は `/validation`、コマンドは **`npm run pilot`**
  （①〜⑧の報告・落ちた理由の8分類・工程ごとの実測・安全条件7項目を一度に出す）。
  **2026-08-20 時点で 操作性 0 / 7 項目・安全条件 6 / 7 項目**（実市場の出品が0件、作業時間が未計測のため）。
  **両方そろうまで100件へ進まない。** 10件の入力は人がやる（無断巡回しない決まりのため自動化しない）。

## 環境

Next.js **15**（App Router）+ React 19 + TypeScript + libsql。
2026-08-20 に 14.2.35 から更新した（14系の最終版で、脆弱性の修正版が14系に存在しなかったため）。

- `searchParams` / `params` は **Promise**。`await` してから使う。
- `next` が抱える `postcss` / `sharp` は `package.json` の `overrides` で修正版に固定してある。
  **外さない**（`npm audit` が0件でなくなる）。

### 実装済みで壊してはいけないもの

- 価格方式は2種類に分かれる。**新品・型番商品＝金額ベースの+5%ステップアップ／中古一点物＝市場相場に対する倍率（Market Premium Ratio）を学習。** 片方に統一しない。
- `TARGET_NET_MARGIN`（15%）は「仕入価格×1.15」ではなく**全費用を引いたあとの純利益率**。必要販売価格の式を単純化しない。
- 除外理由（`SKIP_REASONS`）は画面に日本語で出す。英語コードをそのまま人間に見せない。
- `toHalfWidth` で長音符「ー」を半角ハイフンへ変換しない（商品名が読めなくなる）。
- 取込時に確定した `DUPLICATE` は再分析で上書きしない。
- 画面（クライアント側）から状態ランクを読むときは `lib/conditions.ts` を使う。`lib/normalize.ts` は `node:crypto` を持つのでバンドルできない。

Phase 2（全市場双方向アービトラージ）で追加。**ここも崩さない。**

- **市場は「仕入先」「販売先」に固定しない。** 同じ市場が両方になる。`can_buy` / `can_sell` を勝手に落とさない。
- **「できる」と「してよい」を別の欄に分ける。** 構造＝`can_buy`/`can_sell`、許可＝`terms_status`/`automation_permission`。
  規約で使えない市場も**計算はする。実行だけ止める**（`status='BLOCKED'`）。消すと「取り逃し」が見えなくなる。
- **BUY費用とSELL費用を1つの関数に混ぜない。** `calcAcquisitionCost()` と `calcNetReceipt()` は別。
  BUY側の内訳にSELL専用費用（販売手数料・梱包・保管）を入れない。逆も同じ。
- **BUY費用をすべてゼロにすると Phase 1 の `calcProfit` と純利益が1円単位で一致する。** 式を単純化しない。
- **異常価格チェックは除外条件の一番先に置く。** 後ろに置くとデータ誤りが「最優良Route」として上位に出る。
- **手数料は市場ごと×BUY/SELL別。** 一律◯%にまとめない。`is_estimated=1` のままで自動購入しない。
- **使えない市場（BLOCKED / NOT_ALLOWED）の取り逃しは、実際の相場観測がある時だけ出す。** 参考相場で金額をでっちあげない。
- **利益が最大のRouteをベストにしない。** 利益 × 売却確率 × 資金回転 × リスクで選ぶ。

Phase 3（SHADOW学習・答え合わせ）で追加。**ここも崩さない。**

19. **SHADOWの3テーブルは `ON CONFLICT DO NOTHING`。`DO UPDATE` を書かない。**
    `route_shadow_trades` / `shadow_market_snapshots` / `shadow_evaluations`。
    後から現在値で上書きすると、常に自分が正しかったことになり学習が成立しない。
20. **「売れた」と数えるのは6条件をすべて満たす時だけ。**
    観測あり／`observed_at` あり／観測が判断より後／`price_basis='SOLD_MEDIAN'`／
    `sold_count_30d>=1`／`avg_days_to_sell` あり。
    足りなければ `ACTUAL_SALE_UNCONFIRMED`。**勝ちにも負けにもしない。**
21. **観測できなかった相場は `NO_OBSERVATION` と記録する。参考値で埋めない。**
22. **実績によるスコア補正は件数で強さを変える**（5件未満=補正なし／5〜19=0.3／20〜49=0.6／50以上=1.0）。
    補正幅は上下 **±10点**（`MAX_SCORE_ADJUSTMENT`）。少ない実績で点数を動かさない。
23. **データ信頼度の止め方は2種類あり、混同しない。**
    手数料が概算 → データ信頼度が下がり**そもそも90点に届かない**。
    同一商品未確認 → **90点近くても蓋をして** `WATCH` へ落とす（`confidenceCapped`）。
24. **旧料金で過去利益を再計算しない。** 変更は `fee_change_log` に記録するだけ。
    取引時点の `fee_version`（`f{E|V}-{ハッシュ8桁}`）を保持する。
    Fee Version のハッシュには**金額に影響する14項目だけ**を含める。
25. **`hit_rate` は「売れた割合」ではない。** `AVG(probability_correct)`＝
    売却確率の予測が当たったかどうか。ここを取り違えると集計が全部おかしくなる。
26. テーブル名に注意：**`prediction_accuracy`**（`route_accuracy` ではない）／
    `shadow_market_snapshots` の列は **`role`**（`side` ではない）／
    手数料は **`venue_fee_profiles`** ／ `ownership_state` は **`supplier_products`** にある。
27. `calcFreshness(observedAt, opts)` の第2引数は **オブジェクト** `{freshHours, normalHours, now}`。位置引数ではない。
28. リポジトリのパスに日本語が含まれる。`new URL(...).pathname` はパーセントエンコードされるので、
    **URLオブジェクトをそのまま `fs` に渡す**。

- ルール版は Phase 1 が `v1-`、Phase 2 が `r1-`、Phase 3 が `r2-`。**混ぜない。**

Phase 3.5 / 3.6（実市場検証）で追加。**ここが崩れると集めたデータが全部無意味になる。**

29. **「CSVだから規約上安全」は誤り。** ファイル形式は法的な立場の説明にならない。
    `SELF_OBSERVED`（自分で公開画面を見て記録）と `PROVIDED_BY_VENUE`（事業者から正式提供）を
    **同じ信頼度で扱わない**。`venues.csv_available` は「正式提供があるか」であって
    「取り込める形式か」ではない。**2026-08-20 時点で該当市場は0件。推測で1にしない。**
30. **「ページが消えた＝売れた」にしない。** 消えた・削除・404・非公開・取り下げは全部
    `REMOVED_UNKNOWN`。`CONFIRMED_SOLD` は**売り切れ表示を画面で実際に見たときだけ**。
    読み取れない言葉は `null` を返す（`parseListingState`）。**近い方に寄せない。**
    `SOLD_PROBABLY` のような「たぶん」の状態を作らない。
31. **売れた値段を推測しない。** 状態が `CONFIRMED_SOLD` でなければ成約価格を採用しない。
    確認できなければ `sold_price_known=0` ＝ 画面に「不明」。**出品価格でも0円でも代用しない。**
    一度判明した価格は後の観測で消せない（`COALESCE` / `MAX`）。
32. **`listing_observations` は追記専用。** 同じURLを観測するたび新しい行を積む。
    `UNIQUE(listing_key, observed_at)` ＋ `ON CONFLICT DO NOTHING` は二重取り込み防止であって、
    上書きの許可ではない。
33. **実績値には `real_market_data = 1` の行だけを使う。** テストデータ・記入見本を混ぜない。
    **画面に最初から入っている文字列は「取り込まれる可能性がある」前提で作る**
    （記入見本は `SAMPLE_MARKERS` で弾かれるようにしてある）。
34. **手数料は項目ごとに検証する。** 市場が決めている率（`VENUE_RATE_FIELDS`）だけ `VERIFIED` にできる。
    **送料・梱包費（`ITEM_COST_FIELDS`）は商品と発送方法で変わるので推定のまま据え置く。**
    率だけ確認済みは上限85点＝STRONG BUY(90点)に構造上届かない。
35. **測っていない数字を良く見せない。** 実市場0件は「的中率100%」ではなく **「測定不能」**。
    比較対象が無ければ「まだ比べられません」。**近そうな方を選んで書かない。**
36. **100件そろっても、それだけでは購入しない。必ず14項目の卒業条件で判定する。**
    **メルカリ100件が安定するまで次の市場を追加しない。**
37. **画面（`'use client'`）から読むファイルに `node:` の読み込みを混ぜない。**
    出品追跡の語彙は `lib/listing.ts`（外部依存ゼロ）から読む。`lib/observation.ts` はDB層を抱えている。
    `lib/conditions.ts` と `lib/normalize.ts` の関係と同じ。**テストでは見つからない。`npm run build` で見つかる。**

Phase 3.7（費用の分離・支払方法・検証ファネル）で追加。**ここも崩さない。**

38. **振込手数料を1商品ごとの販売費用として引かない。**
    メルカリの200円は「振込申請1回ごと」であって商品1件ごとではない。
    `fixed_fee`（1取引ごと）と `payout_fee_fixed`（申請1回ごと）を必ず分ける。
    **`fixed_fee` に200円を戻さない。** 戻すと1,000円の商品で費用を100倍見積もることになる。
39. **按分した振込費用（`payout_allocated_net_profit`）を判定に使わない。**
    `decision` と `route_score` は按分前の `expected_net_profit` で決める。
    按分は「参考」表示のみ。分母（`PAYOUT_ITEMS_PER_REQUEST`）を変えても判定が動いてはいけない。
40. **分からない支払手数料を0円として足さない。** `fee: null` は「不明」。
    不明のまま STRONG BUY にしない（`buy_payment_fee_known = 0`）。
41. **いちばん安い支払方法をシステムが勝手に選ばない。**
    使うのは設定 `DEFAULT_MERCARI_BUY_PAYMENT_METHOD`＝**実際に自分が使う支払方法**。
    `cheapest` / `lowest` / `best` のような選択関数を作らない（受け入れテストで検査している）。
42. **費用の確からしさ（`COST_CONFIDENCE`）を100点にしない。** 上限は `COST_CONFIDENCE_MAX = 95`。
    一部の費目が公式確認済みでも、全体を「完全確認済み」と呼ばない。
43. **決済手数料の「0円」を未確認のまま通さない（BUY側）。**
    支払方法で変わる費用なので、0でも確認を要求する（`NEVER_ASSUME_ZERO_FIELDS`）。
    そのためメルカリBUYの手数料は `ESTIMATED` のまま。**これを VERIFIED に上げない。**
44. **「100件登録した」を成果にしない。** 見るのは検証ファネル
    （観測 → 商品一致 → Route成立 → 答え合わせ対象 → STRONG BUY候補）と
    `MATCHABLE_RATE` / `ROUTE_GENERATION_RATE` / `SHADOW_ELIGIBLE_RATE`。
    **候補を増やすために判定を甘くしない。手数料の実額確認と仕入先を増やす方で増やす。**
45. **100件の前に10件（PILOT）。** 7項目すべて満たすまで100件へ進まない。
    この10件は「データを集めるため」ではなく「**どこを自動化するか決めるため**」に入れる。
    いちばん時間がかかった工程（`observation_work_log`）が次に自動化する場所。
46. **決済・購入・注文・発送を思わせる名前のテーブルを作らない。**
    支払方法ごとの手数料表は `venue_method_fee_rules`（`venue_payment_methods` ではない）。
    受け入れテストの「実行系テーブルが存在しない」検査を緩めるのではなく、名前の方を実態に合わせる。

Phase 3.8（まず10件のPILOT・実測で次に作るものを決める）で追加。**ここも崩さない。**

47. **落ちた理由に「その他」を作らない。** 8分類（`MODEL_NUMBER_MISSING` / `NO_SELL_MARKET` /
    `PRODUCT_MATCH_FAILED` / `MARKET_DATA_MISSING` / `FEE_UNVERIFIED` / `SHIPPING_UNKNOWN` /
    `INSUFFICIENT_MARGIN` / `LOW_DATA_CONFIDENCE`）のどれかに必ず入れる。
    「その他」があると面倒なものが全部そこへ入り、何を直せばいいか分からなくなる。
    **`INSUFFICIENT_MARGIN` が多いときに閾値を下げない。仕入先を増やす**（ルール13）。
48. **同一商品の答え合わせは3択だけ。「たぶん合っている」を作らない。**
    `CORRECT_MATCH` / `INCORRECT_MATCH` / `UNCERTAIN`。
    迷ったものは `UNCERTAIN` に置き、合格へ寄せない（迷うもの＝紛らわしいもの＝いちばん危ない）。
    **`INCORRECT_MATCH`（取り違え）は1件でも重大。** 見逃しは儲け損ねだけだが、
    取り違えは実際に金を失い在庫も残る。扱いを対称にしない。
49. **人の答えで機械の判定を上書きしない。** `tracked_listings.identity_key` はそのまま残し、
    人の答えは `identity_match_reviews` に別で持つ。上書きすると
    「機械が何回間違えたか」が数えられなくなり、精度を測れなくなる。
    **「違う商品だった」を選んだときは理由の記入を必須にする**（空欄では保存させない）。
50. **時間が長い工程＝次に自動化すべき工程、ではない。**
    「商品発見」「商品情報確認」は最も時間がかかるが、無断巡回しない決まりのため自動化できない。
    秒数だけで並べると、この2つが1位になって誤った結論が出る。
    必ず **① かかっている時間の順（事実）と ② 自動化して効く順（時間×自動化しやすさ）の
    両方**を出し、②を「次に作るもの」にする。`AUTOMATION_ROI` を
    「全部自動化して0時間」にしない。測れないときは数字を作らず「計算できません」と書く。
51. **記入見本・テスト用の行に書かれた作業秒数を「実測」に混ぜない。**
    出品は実市場データ判定で弾かれても、作業ログの秒数は残る。混ぜると誰も作業していないのに
    工程の順位が出て、それを根拠に開発方針を決めてしまう（ルール33・35違反）。
    出品1件ごとの記録（`batch_id` が `LISTING:`）は **その出品が `real_market_data = 1` のときだけ数える**。
    **除外した分は黙って引き算せず、「◯分は数えていません」と画面に書く。**
52. **データの点検で見つけたものを、勝手に消したり直したりしない。** 人に見せて、人が直す。
    自動で消すと「なぜ汚れたか」が分からなくなる。
53. **100件へ進む判定は「操作性7項目」と「安全条件7項目」の両方がそろって初めて合格。**
    片方だけで進まない。**まだ1件も確認していない状態を、黙って合格にしない**
    （`unmeasurable` を持ち、○にしない）。合格できないときに条件の方をゆるめない。
54. **受け入れテストのデータに実在の型番を使わない。** `P38TESTONLY001` のようなテスト専用の
    型番にして、後片付けを型番の文字列で確実に行う。実在の型番で消すと本物の相場を巻き添えにし、
    形が違うと消し残してデータベースを汚す（実際に起きた）。

## ポート

3910（`amazon-ai-seller-factory` の 3900 と衝突させない）
