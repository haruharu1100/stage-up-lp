# AI Commerce OS — AI向け作業ルール

## 作業前に必ず読む（順番も守る）

1. `../事業Vault/AI Commerce OS/00_設計書v1_最上位.md`
2. 同フォルダ内の全ファイル
   - 01_DB設計案 / 02_Phase0_外部連携調査 / 03_安全設計と事故防止 / 04_ロードマップ / 05_Obsidianナレッジ構造
   - 06_商品大量生成とパイプライン / 07_価格戦略設計 / 08_仕入先ネットワークと勝ち筋拡張
   - 09_Phase1実装記録 / 10_Phase2実装記録 / 11_Phase3実装記録 / 12_Phase3.5実装記録
     / 13_Phase3.6実装記録 / 14_Phase3.7実装記録 / 15_Phase3.8実装記録
   - **33_Phase6.5_候補の採点とTOP5 / 31_Phase6.5_仕入データ源の探索_採点基準 / 32_問い合わせ文案_YahooとRakuten**
     （**いまの最前線はここ。仕入先・データ源の話をする前に 33 → 31 の順で必ず読む。
     63候補を調べて、4項目すべてYESの候補は0件。だから Live Connector 実装には進んでいない。
     TOP1は NETSEA（80点）だが、規約第8条2項の書面承諾が未取得。楽天の懸念条項は2つではなく5つ**）
   - **30_Phase6設計_FIRST_SUPPLIER_CONNECTOR**
     （**LEGAL_USAGE_GATE の本体。外部市場へ繋ぐ話・利用可否に触る前に必ず読む。
     Yahoo!もeBayも BLOCKED で、通信は1回もしていない。「たぶん使える」で繋がない**）
   - **29_Phase5設計_AUTO_RESEARCH_ORCHESTRATOR**
     （**主経路が「人が10件入れる」から「AIが自動で探す」へ変わった回。
     自動リサーチ・Connector・候補キューに触る前に必ず読む**）
   - **28_Phase4設計_Supplier to Amazon Route Validation**
     （**仕入価格→Amazon販売のRouteに触る前に必ず読む。Phase 5 の下地はここ**）
   - **16_Phase3.9設計_自動収集と購入ページ導線 / 17_市場別_正規データ取得調査 / 18_Phase3.9実装記録
     / 19_Connector再調査_市場別スコア**
     （**外部市場へ繋ぐ話をする前に 19 → 17 の順で必ず読む。「たぶんAPIがある」で実装を始めない。
     17 と 19 が食い違う箇所は 19 が正しい**）
3. `../事業Vault/DIGEST.md`

矛盾したら **最新のユーザー指示 > DIGEST > 設計書フォルダ** の順で優先する。

## 仕様変更の扱い

新しい指示はその場限りにせず、**必ず `00_設計書v1_最上位.md` へ追記・統合する**。
メモを増やして仕様を分散させない。古い記述と矛盾したら古い方を書き換える。

## 絶対に崩さないルール

```
正確性 > 安全性 > 利益 > 自動化率 > 商品数
```

1. **無断で外部サイトを自動操作しない。** 公式API / 法人API / データフィード / CSV / Webhook / 規約上認められた方法のみ。
   使えないなら機能を落として `CONNECTOR_NOT_AVAILABLE` / `MANUAL_APPROVAL_REQUIRED` を返す。**スクレイピングで埋めない。**
2. **Connector の capability が `api` でなければ、自動実行を実行層が拒否する。** 環境変数で true にしても動かさない。
3. **金銭を動かす処理は必ず Action Gate を通す。** 直接 Connector を呼ぶ実装を書かない。
4. **idempotency_key に UNIQUE 制約。** 購入・出品・発送・注文取込の4つは必須。
5. **在庫は個体単位。** SKU×数量方式にしない（二重販売の原因）。
6. **外部APIの「応答不明」は再送しない。** 人間へエスカレーションする。
7. **LLMに金額計算をさせない。** 利益・ROI・スコア・資金配分は決定的なコードで計算する。LLMは説明と分類のみ。
8. **LLM出力は必ずスキーマ検証。** 落ちたら SKIP（Fail Closed）。
9. **AIは自分の閾値・予算・自動化モードを変更できない。** 提案のみ。変更は人間。
10. **APIキーをコードに書かない。** `.env` / Secret Manager のみ。
11. **既定は `COMMERCE_AUTO_MODE=SAFE` / `SANDBOX=true`。** 勝手に変えない。
12. **虚偽・誇大な商品説明を生成しない。** 景表法・古物営業法を守る。
13. **商品数を増やすために判定を甘くしない。** しきい値を下げるのではなく仕入先を増やす。
14. **重複防止3層（L1 出品重複 / L2 個体重複 / L3 商品重複）を必ず通す。** L1はDBのUNIQUE制約で担保する。
15. **LLM判定は段階的コストフィルタの最終段（L4）でのみ呼ぶ。** 想定粗利がLLM費用の20倍未満なら呼ばない。
16. **値上げは必ず上限クランプを通す。** 相場上限・`MAX_PRICE`・`MAX_STEP_UPS`・売却確率下限のいずれかに触れたら値上げしない。
17. **未定義の商品状態ラベルを推測でマッピングしない。** 共通6段階（N/S/A/B/C/D）に無ければ `MANUAL_REVIEW`。
18. **商品マスタの統合は `merged_into` で行い、削除しない。** 実行は人間のみ。

## Fail Closed の3原則

- 分からないなら買わない
- データがおかしいなら止まる
- APIがおかしいなら止まる

## 現状

**Phase 6.5（SUPPLIER DATA SOURCE DISCOVERY）調査完了（2026-08-26）。コード実装は増やしていない。**

> [!danger] Phase 6.5 の結論：**63候補を調べて、今すぐ正式接続できる候補は0件。**
> 合格ライン＝`COMMERCIAL_USE_ALLOWED` / `INTERNAL_BUSINESS_USE_ALLOWED` / `AUTOMATED_RETRIEVAL_ALLOWED` / `PURCHASABLE_PRODUCTS` が**すべてYES**。
> **1件も満たしていないので、Live Connector の実装には進まない。**
> ただし**あと1通の書面承諾で開く候補が1件**＝**NETSEA Buyer API（80点）**。
> 規約第8条2項「情報を加工する場合は事前に書面承諾」が未取得で、
> かつ第2条（商用目的を認める）と第5条3（使用から収入を得ることを禁止）が文言上ぶつかっている
> → **ルール70で両論のまま記録し、厳しい側で設計**（`COMMERCIAL_USE_ALLOWED` は UNKNOWN へ倒す）。
> TOP5＝NETSEA 80／バリューコマース 72／BigBuy 71／Datafeedr 61／vidaXL 58。
> 6位 orosy 57 は**規約をブラウザで読むだけで1位と入れ替わり得る**（HTTP取得ではSPAで本文に到達できない）。
> 詳細は `33_Phase6.5_候補の採点とTOP5.md` / 基準は `31_Phase6.5_仕入データ源の探索_採点基準.md`。

> [!danger] Phase 6.5 で判明した、設計を変える4つの事実
> 1. **「Amazonに出していいか」は卸モールの規約では決まらない。**ほぼ全て「サプライヤー／出展企業ごとの個別条件」。
>    → **モール単位で可否を持つ設計は誤り。サプライヤー単位・商品単位で持ち、取れなければ「判定不能」で止める。**
> 2. **中国系B2Bは7件すべてJAN非保有**（AliExpress／Alibaba／1688／DHgate／Made-in-China／Global Sources／Temu）。
>    既存Amazon商品との機械突合が**原理的に不可能**。→ 欧州系卸（EAN-13保有）が唯一の構造的な解。
> 3. **海外卸は関税・輸入消費税を事前算出できる候補がゼロ。**
>    → 利益計算式に「輸入コストを人が入力する欄」を設けない限り、海外卸は利益判定が成立しない。
> 4. **SP-API を接続すると Keepa との関係で AUP 4.3 の名宛人になる**
>    （「Amazonのウェブサイトから取得した情報を販売する外部データサービスを、使用・提供・宣伝してはならない」）。
>    Keepa はまさにこれに当たり得る。**SP-API 導入前に必ず照会する。**

> [!warning] 3市場の扱い（変えない）
> - **Yahoo!ショッピング** = `BLOCKED_PENDING_CONFIRMATION`。回答がYESなら**即座に第1候補として再評価**する
> - **楽天市場** = `EXPLICIT_PERMISSION_REQUIRED`。**禁止と断定はしないが、許可取得前にLive Connectorにしない**。
>   懸念条項は2つではなく**5つ**（第10条(4)(10)には「明示的に許可した場合を除く」があるが、(7)(9)と第8条4項には**無い**）
> - **eBay** = 優先度：低。**削除はしない**

> [!danger] 採用しないと決めたもの（金を払っても駄目）
> **スクレイピング代行型（Rainforest API / Bright Data / Oxylabs）は採用不可。**
> 「提供会社が適法だと述べていること」と「利用者が対象サイトの規約に違反しないこと」は**別の問題**。
> Rainforest は自ら「Amazonの提供するAPIは使わずWebスクレイピングしたデータ」と明言、
> Bright Data のライセンスは適法性の判断責任を顧客側に置き、Oxylabs は ToS 本文に到達すらできない。
> 同じ理由で **Amazon PA-API / Creators API も不可**（集約・分析・価格比較・LLM学習が明文で禁止、ASIN以外は24時間で破棄義務）。

**以下は Phase 6（FIRST SUPPLIER CONNECTOR、2026-08-26 実装済み）の内容。**

> [!danger] Phase 6 でいちばん大事なこと：**技術は出来た。許可が取れていない。**
> **`LEGAL_USAGE_GATE = BLOCKED`（Yahoo!ショッピング・eBay の2市場とも）。外部への通信は0回。**
> Yahoo!のFAQに「**非商用目的のみ**」と「**商用をすべて禁じるものではない**」が併記されており、
> 当社の用途が入るか公式の文だけでは決まらない（10項目のうち**6項目がUNKNOWN**）。
> **次の一手は人がYahoo!へ問い合わせること。`CONTACT_VENUE_BY_AI_ALLOWED = false`＝AIは外部へ連絡しない。**
> 詳細は `30_Phase6設計_FIRST_SUPPLIER_CONNECTOR.md`。

Phase 6 で足したのは、**「その市場の口を使ってよいか」を先に決める門**（`lib/phase6/legalgate.ts`）と、
門が開いたときに動く仕入側の一式（読み取り・同一商品判定・順位付け・段階Gate・ファネル/KPI・Route配線）。
**順番を逆にした＝規約の確認 → 門を作る → 門が開いた市場だけ実装。**
先に繋ぐと、後から「駄目でした」と言われても取り消せないため。

- **UNKNOWN を YES 扱いしない。登録の無い市場は通さない（Fail Closed）。**
  **「禁止と書かれていない」を、やってよい根拠にしない。**
- **判定には一次資料の原文引用＋出典URL＋確認日が必須。根拠の無いYESはYESと数えない**
  （`effectiveValue()` が UNKNOWN へ落とす）。
- **Rate Limit の数字が食い違ったら、全部残して一番厳しい値で設計する**（Yahoo!は2秒間隔）。
- **eBayは当社の設計と根本的に相性が悪い**：①日本のマーケットプレイスが無い
  ②取得データをAI学習へ入れることを条文が名指しで禁止（＝Phase 3 のSHADOW学習へ流し込めない）
  ③他社データと同じ画面で混ぜてはならない（Public Display）。**Yahoo!が通らなかった場合の代替に留める。**
- **安全装置はフラグではなく「コードが無い」**：`YAHOO_SHOPPING_LIVE_FETCH_IMPLEMENTED = false` ／
  `EBAY_BROWSE_LIVE_FETCH_IMPLEMENTED = false` ／ `AUTO_SUPPLIER_RESEARCH = false` ／
  `AUTO_PURCHASE_IMPLEMENTED = false` ／ `SCRAPING_IMPLEMENTED = false`。
- **47件へ一気に当てない。段階Gate 1件 → 5件 → 10件。同時接続は1市場まで。**

以下は Phase 5 までの範囲（**そのまま生きている。削らない**）。

> [!danger] いちばん上の方針（2026-08-25 に変わった）
> **`PRIMARY WORKFLOW = AUTO RESEARCH` ／ `MANUAL INPUT = FALLBACK`。**
> ご本人の言葉：「**人が仕入商品を10件入れる」方式は検証用に残すだけにして、
> 完成形はAIが勝手に商品候補を探し、利益が出るRouteを作り、あなたは上位候補だけ見る形にします。**」
> **Phase 4a の10件入力機能は削除していない。`MANUAL_SUPPLIER_FALLBACK` として残してある。**
> 詳細は `../事業Vault/AI Commerce OS/00_設計書v1_最上位.md` §0.5・§15.14 と
> `29_Phase5設計_AUTO_RESEARCH_ORCHESTRATOR.md`。

Phase 5 で足したのは、**人の操作なしで 市場データ取得 → 候補発見 → 同一商品照合 →
BUY/SELL Route → 需要 → 競合 → 費用 → 利益 → 売却確率 → 資金回転 → リスク → BUY候補ランキング
までを一本につなぐ司令塔**（10工程）。
コマンドは **`npm run phase5:research`**、画面は **`/research`**（毎朝これだけ見る）。

- **市場は `VENUE` として一律に扱う。KOMEHYO専用・BOOKOFF専用・メルカリ専用を作らない。**
  **Venueは買う側にも売る側にもなる。方向を固定しない**（Phase 2 のルールの続き）。
- **取得手段は7段階**（`OFFICIAL_API` / `BUSINESS_API` / `PARTNER_API` / `OFFICIAL_DATA_FEED` /
  `OFFICIAL_CSV` / `AUTHORIZED_INTEGRATION` / `MANUAL_FALLBACK`）。
  **スクレイピングはこの7段階に「選択肢として存在しない」**（禁止と書く以前に、実装が無い）。
- **候補キューは8状態**（`NEW` / `FILTERED` / `MATCHING` / `ANALYZING` / `ROUTE_READY` /
  `OPPORTUNITY` / `REJECTED` / `WATCHING`）。
- **実測（2026-08-25 の実行結果・作り話ではない）**：
  読んだ商品 **100件** ／ 記入見本として除外 **0件** ／ ふるいを通った **47件** ／
  到達した工程 **4工程目（安い計算でふるいにかける）まで** ／ **同一商品の照合で停止** ／
  仕入先探し待ちに退避 **47件** ／ **購入候補 0件** ／
  **外部への通信 0回 ／ Keepaの枠 0 ／ AI費用 0円。**
- **止まっている理由はただ1つ：仕入側（買う側）に正式接続できているConnectorが0件**
  （販売側は1件＝Keepa、名前だけ挙がっている候補が13件）。
  **能力不足ではない。「買える市場の正式な口が無い」だけ。**
  次に効く一手は **仕入側のConnectorを1つ正式につなぐこと**（つないだ瞬間、退避中の47件が前へ進む）。
- **自動購入・自動出品・自動決済・自動発送は、いまも「コードごと存在しない」**（フラグOFFではない）。

以下は Phase 4a までの範囲（**そのまま生きている。削らない**）。
範囲は 取得 → 比較 → Route生成 → 利益計算 → SHADOW記録
→ 7/30/90日の答え合わせ → 予測誤差 → Route勝率 → 実績によるスコア補正
→ 手数料の確からしさ判定 → 実市場データ判定 → 3本立て予測（RAW/CALIBRATED/CONSERVATIVE）
→ 誤りの種類分け（FP/FN） → STRONG BUY 的中率 → Phase 4 卒業条件の機械判定
→ メルカリの実出品を1件ずつ追跡（初回/24h/3日/7日/14日/30日）＋人間の作業時間の実測
→ 取引費用と振込費用の分離／支払方法ごとの手数料／費目ごとの確からしさ／検証ファネル
→ まず10件のPILOT（落ちた理由の8分類／同一商品の答え合わせ／工程ごとの実測から
次に自動化する工程を決める／データ汚染の点検／100件へ進む安全条件7項目／①〜⑧の報告）
→ 正規Connectorへの一本化（取得手段7段階・状態10種）と「購入ページを開く」導線（`/buy`）
→ 市場ごとの「許可」を8項目に分けて管理・13接続先のCONNECTOR PRIORITY SCORE（`/venues`）
→ 売れるかテスト（`/sellability`）＝人が Keepa 等で見た数字を書き写し、売れているかだけを判定
→ KEEPA_READ_ONLY（`/keepa`）＝Keepa APIから1回1件だけ読み取り、保存→正規化→売れるか判定→
枠（Token）の消費確認までを社内検証としてやる
→ **ASINの出どころ管理（Phase 3.11）＝ASINを「どこから手に入れたか」を必ず記録し、
出どころが確かなときだけAmazon公式形式の商品ページURLを組み立てる**
→ **仕入価格 → Amazon販売のRoute検証（Phase 4a）＝仕入先の商品を起点に、
同一商品の関門 → Amazon想定販売価格（理論値と保守値の2本立て） → Amazon費用と当社費用を分けた計算
→ 保守純利益・保守ROI・いくらまでなら買えるか → BUY / WATCH / SKIP → 仕入先ページを開く
までを一本につなぐ** まで。
実購入・実出品・実決済・実発送・外部サイトへの自動ログイン・自動値上げ・無許可スクレイピングは
**コードごと存在しない**（フラグOFFではなく未実装）。
所有していない商品の先行販売もしない（`ownership_state` は全件 `NOT_OWNED`）。

**10件PILOT（Phase 3.8）はユーザー指示19により保留中。** 仕組みは消さない。再開は人が決める。

- 実装記録：`09_Phase1実装記録.md` → `10_Phase2実装記録.md` → `11_Phase3実装記録.md`
  → `12_Phase3.5実装記録.md` → `13_Phase3.6実装記録.md` → `14_Phase3.7実装記録.md`
  → `15_Phase3.8実装記録.md` → `18_Phase3.9実装記録.md`
  → `19_Connector再調査_市場別スコア.md`
  → `20_Keepa公式API調査.md`
  → `21_Phase3.10実装記録_KEEPA_READ_ONLY.md`
  → `23_Phase3.11実装記録_ASINの出どころ管理.md`
  → `24_Phase3.12実測記録_Keepa5件テスト.md`
  → `25_Phase3.13記録_画像の読み取りと需要指標の食い違い.md`
  → `26_Phase3.14実測記録_Keepa20件テスト.md`
  → `27_Phase3.15実測記録_Keepa100件テスト.md`
  → **`28_Phase4設計_Supplier to Amazon Route Validation.md`**
  → **`29_Phase5設計_AUTO_RESEARCH_ORCHESTRATOR.md`**
  → **`30_Phase6設計_FIRST_SUPPLIER_CONNECTOR.md`**（`../事業Vault/AI Commerce OS/`）
- 受け入れテスト：`npm run test:phase1`（66項目）／`npm run test:phase2`（50項目）／
  `npm run test:phase3`（113項目）／`npm run test:phase3-5`（112項目）／
  `npm run test:phase3-6`（107項目）／`npm run test:phase3-7`（122項目）／
  `npm run test:phase3-8`（128項目）／`npm run test:phase3-9`（113項目）／
  `npm run test:phase3-9b`（147項目）／`npm run test:phase3-9d`（86項目）／
  `npm run test:phase3-10`（314項目）／`npm run test:phase3-11`（179項目）／
  `npm run test:phase3-12`（201項目）／
  `npm run test:phase3-13`（131項目）／
  `npm run test:phase3-14`（151項目）／
  `npm run test:phase3-15`（245項目）／
  `npm run test:phase4`（334項目）／
  `npm run test:phase5`（311項目）／
  **`npm run test:phase6`（414項目）**。
  **合計3,324項目（19本）。**
  **仕様を変えたら18個とも通す。1つだけ通して満足しない。あわせて `npm run build` も通す**
  （画面のバンドルエラーはテストでは見つからない）。
- **受け入れテストが自分の安全装置を落としたときは、安全装置ではなくテストの方を直す。**
  ただし直してよいのは「**テストが事実を取り違えているとき**」だけ。
  **実際に8件あった。うち6件は同じ形の間違いで、「歯止めをコメントで説明したら、
  その説明文にテストが反応して落ちた」というものである。**
  **5件目は別の形で、「いまは段階S2」を合格条件にしていたため、
  人がS3へ進めた瞬間にテストが落ちた**（2026-08-25・`test-phase3-12.ts`）。
  **7件目も同じ形で、「いまは段階S3」を合格条件にしていたため、
  人がS4（100件）へ進めた瞬間に落ちた**（2026-08-25・`test-phase3-14.ts`）。
  **8件目は「扱い済みの形の違いはちょうど3か所」を合格条件にしていたため、
  100件取得が設計どおり止まって、その原因を調べて扱いを決めた瞬間に落ちた**
  （2026-08-25・`test-phase3-15.ts`）。→ 「3か所以上」に直した。
  **見張りたいのは件数ではなく「扱いを決めていない違いを黙って通さないこと」**であり、
  それは `NEW_SCHEMA_DRIFT` で取得が止まることの方が見張っている。
  **段階が正しく進むと不合格になるテストは逆さま**なので、
  「S2以上まで来ていること」「上限が段階の表と一致していること」に直した。
  守りたいのは「S2固定」ではなく「**AIが自分で段階を進めないこと**」であり、
  それは `KEEPA_AUTO_ADVANCE_STAGE === false` の方が見張っている。
  （①ログイン情報入りURLを弾く `u.password` を「ログイン情報を扱っている」と誤判定
  ②`lib/autopurchase.ts` に引用したユーザー指示の原文に反応
  ③`lib/keepa/policy.ts` の「`process.env` では変えられない」という説明文に反応
  ④`scripts/keepa-one.ts` の「`--source=` と書くだけで名乗れてしまう」という説明文に反応）。
  → **ソース全文を正規表現で検査するときは、先にコメントを取り除く。**
  **直した理由は必ずコメントに残す。**
- **テストが全部通っても画面は見る。** 2026-08-25、`/buy` に記入見本（サンプル）由来の
  仕入先商品がRoute判定まで通って並んでいた。**334項目のテストは全部通っていた。**
  見つかったのは画面を開いたときである。直したのは3か所：
  ①取り込み時に `SAMPLE_MARKERS` で印を付ける ②`saveOfferRoute` は印の付いた行を保存しない
  ③画面と集計から印の付いた行を外す。**サンプル由来の判定4件と一致候補3件は人へ報告してから消し、
  Keepa 100件のデータには一切触れていない**（ルール52）。
- **仕入先商品を入れて判定する（Phase 4a）**：
  `npm run phase4:template`（記入用のCSVの見本を出す）→ `npm run phase4:import`（取り込み）
  → `npm run phase4:route`（同一商品の関門〜利益〜BUY判定）→ `npm run phase4:confirm`（人が確認）。
  画面は **`/buy`** の上の表（14列）。**まず10件。`PHASE4_SUPPLIER_OFFER_LIMIT` で件数を縛ってある。**
  **いきなり100件入れない。どこで落ちたかをファネルで見てから増やす。**
- 仕入候補と「購入ページを開く」：画面は **`/buy`**。ボタンは**新しいタブで開くだけ**。
  **2026-08-20 時点で、開ける候補0件・押した回数0件・`CONNECTED` の市場0件。**
- Keepa から1件だけ取る：**`npm run keepa:one -- --asin=（10桁のASIN）`**。画面は **`/keepa`**（見るだけ）。
  取ったあとは必ず止まり、5件へは自動で進まない。
  **投げる前に必ず `npm run keepa:token` を通す**（キーが有効かと残り枠だけを確かめる。
  Keepa の `token` は**枠を消費しない**ので、間違ったキーで枠を溶かす事故が消える）。
  **2026-08-25 に初回取得を実施（B0978NB1VQ・1件・実消費1）。**
  **同日、第2実測を実施（0747554560・1件・実消費1）。** 残り1,188／補充20（毎分）／上限1,200。
- **候補ASINを1件だけ正式に選ぶ：`npm run keepa:discover`**（`-- --dry` で通信ゼロの下見）。
  Keepa の商品検索（Product Finder）を1回だけ呼び、返ってきた実在ASINの中から1件を選ぶ。
  **枠は11（10＋結果100件ごとに1）。`KEEPA_MAX_DISCOVERY_TOKENS = 15` を超える条件は投げない。**
  **候補を広めに出すときは `KEEPA_DISCOVERY_PER_PAGE_WIDE = 300`（＝10＋3＝13枠）を使う。
  上限15にちょうど届く500件（15枠）は使わない（ぎりぎりに置かない）。**
  この枠は `keepa_token_usage.purpose = 'DISCOVERY'` として、商品取得（`PRODUCT_FETCH`）と
  **別に記録する**（1商品あたりの実費がぼやけるため混ぜない）。
  返ってきた候補は**全部** `keepa_asin_candidates` に保存し、選んだ1件に `chosen = 1` を立てる。
  **1件も返らなくても条件を緩めない**（緩めれば必ず何か返るが、それは条件を満たした商品ではない）。
- **取ったデータの検算：`npm run keepa:audit`**（一覧）／`npm run keepa:audit -- --asin=（ASIN）`（1件）。
  **Keepa へ一切通信せず、枠を1つも使わず、APIキーも読まない。**
  使うのは `keepa_raw_responses` に保存済みの生の応答だけ。
  読み取り方を直したとき、取り直して確かめると①枠を使う②その間に相場が動く の2つが起きて、
  「直ったから値が変わった」のか「相場が動いたから変わった」のかが区別できなくなる。
  生データを残してあるのはこのためで、**修正の検算は必ずこちらで行う**。
- **20件テスト（Phase 3.14）：`npm run keepa:twenty`**（`-- --dry` で通信ゼロの下見）。
  **保存済みの件数を数えて、足りない分だけ取る**（20件のうち7件が保存済みなら13件だけ取る）。
  **分析は `npm run keepa:study`。Keepaへ通信せず、枠を1つも使わない。**
  **2026-08-25 実施。追加13件・合計20件。枠は 候補探し14／下見13／深追い0／合計27
  （見積り27・差0）。1件あたり実費 1.35枠。読み取り不具合0件。**
- **100件テスト（Phase 3.15）：`npm run keepa:hundred`**（`-- --dry` で通信ゼロの下見）。
  **単に80件増やすのではなく、売り場8区分の配分表（`lib/keepa/strata.ts`）に沿って、
  足りない区分から先に取る**（20件時点で本が10/20＝50%を占めていた偏りを直すため）。
  1区分につき商品検索は不足数ぶんだけ。**区分ごとに試行回数と枠に上限があり、
  見つからなければ条件を緩めず `TARGET_NOT_REACHED` として正直に終える。**
  **保存済みASINは選ぶ前に外す**ので、既存20件は取り直さない（ルール88）。
  **20→40→70→100 の区切りで必ず立ち止まる。**7つの異常（読み取り不具合／新しい形の違い／
  枠のズレ／日本以外／実在しないASIN／キー漏れ／不明率の急変）があれば自動で停止する。
  **2026-08-25 実施。3回に分けて追加80件・合計100件。
  枠は 138＋135＋83＝合計356（深追いは3回とも0）。1件あたり実費 4.45枠。
  40件の区切りと70件の区切りで、設計どおり2回止まった（下記）。
  100件到達時は7条件すべて異常なし。**
  **分析は `npm run keepa:study`。Keepaへ通信せず、枠を1つも使わない。**
  **段階は S4（100件）。次の段階へは `KEEPA_CURRENT_STAGE` を人が書き換えないと進まない。**
- 費用の計算を変えたら `npm run fees`（手数料の版と状態の再計算）→ `npm run routes`（Route再生成）
  を順に流す。流さないと、古い費用で作られたRouteが残って受け入れテストが落ちる。
  **`npm run routes` は `best_route_id` を全行書き直す**（ベストが無くなった商品は番号も消える）。
  2026-08-25 に、ここを「ベスト市場がある商品だけ」に絞っていたせいで
  古い番号が6件残り、SKIP になったRouteを指していたのを直した。
- 日次レポート：`npm run report`（結論1行＋卒業条件の内訳）。画面は `/readiness`。
- 実市場での答え合わせ：`npm run validation`。画面は `/validation`。
- 市場ごとの取得可否の確認：`npm run observe`（`--venue=X` で1市場ずつ）。
- Phase 4 へ進む前に `04_ロードマップ.md` の卒業条件を確認する。
  **2026-08-20 時点で卒業条件は 0 / 14 項目**
  （実市場の出品 **0 / 100件**、手数料の実額確認 **1 / 18件**、STRONG BUY 的中率 **測定不能**、作業時間 **未計測**）。
- **100件の前に、まず10件のPILOTを通す。** 画面は `/validation`、コマンドは **`npm run pilot`**
  （①〜⑧の報告・落ちた理由の8分類・工程ごとの実測・安全条件7項目を一度に出す）。
  **2026-08-20 時点で 操作性 0 / 7 項目・安全条件 6 / 7 項目**（実市場の出品が0件、作業時間が未計測のため）。
  **両方そろうまで100件へ進まない。** 10件の入力は人がやる（無断巡回しない決まりのため自動化しない）。

## 環境

Next.js **15**（App Router）+ React 19 + TypeScript + libsql。
2026-08-20 に 14.2.35 から更新した（14系の最終版で、脆弱性の修正版が14系に存在しなかったため）。

- `searchParams` / `params` は **Promise**。`await` してから使う。
- `next` が抱える `postcss` / `sharp` は `package.json` の `overrides` で修正版に固定してある。
  **外さない**（`npm audit` が0件でなくなる）。

### 実装済みで壊してはいけないもの

- 価格方式は2種類に分かれる。**新品・型番商品＝金額ベースの+5%ステップアップ／中古一点物＝市場相場に対する倍率（Market Premium Ratio）を学習。** 片方に統一しない。
- `TARGET_NET_MARGIN`（15%）は「仕入価格×1.15」ではなく**全費用を引いたあとの純利益率**。必要販売価格の式を単純化しない。
- 除外理由（`SKIP_REASONS`）は画面に日本語で出す。英語コードをそのまま人間に見せない。
- `toHalfWidth` で長音符「ー」を半角ハイフンへ変換しない（商品名が読めなくなる）。
- 取込時に確定した `DUPLICATE` は再分析で上書きしない。
- 画面（クライアント側）から状態ランクを読むときは `lib/conditions.ts` を使う。`lib/normalize.ts` は `node:crypto` を持つのでバンドルできない。

Phase 2（全市場双方向アービトラージ）で追加。**ここも崩さない。**

- **市場は「仕入先」「販売先」に固定しない。** 同じ市場が両方になる。`can_buy` / `can_sell` を勝手に落とさない。
- **「できる」と「してよい」を別の欄に分ける。** 構造＝`can_buy`/`can_sell`、許可＝`terms_status`/`automation_permission`。
  規約で使えない市場も**計算はする。実行だけ止める**（`status='BLOCKED'`）。消すと「取り逃し」が見えなくなる。
- **BUY費用とSELL費用を1つの関数に混ぜない。** `calcAcquisitionCost()` と `calcNetReceipt()` は別。
  BUY側の内訳にSELL専用費用（販売手数料・梱包・保管）を入れない。逆も同じ。
- **BUY費用をすべてゼロにすると Phase 1 の `calcProfit` と純利益が1円単位で一致する。** 式を単純化しない。
- **異常価格チェックは除外条件の一番先に置く。** 後ろに置くとデータ誤りが「最優良Route」として上位に出る。
- **手数料は市場ごと×BUY/SELL別。** 一律◯%にまとめない。`is_estimated=1` のままで自動購入しない。
- **使えない市場（BLOCKED / NOT_ALLOWED）の取り逃しは、実際の相場観測がある時だけ出す。** 参考相場で金額をでっちあげない。
- **利益が最大のRouteをベストにしない。** 利益 × 売却確率 × 資金回転 × リスクで選ぶ。

Phase 3（SHADOW学習・答え合わせ）で追加。**ここも崩さない。**

19. **SHADOWの3テーブルは `ON CONFLICT DO NOTHING`。`DO UPDATE` を書かない。**
    `route_shadow_trades` / `shadow_market_snapshots` / `shadow_evaluations`。
    後から現在値で上書きすると、常に自分が正しかったことになり学習が成立しない。
20. **「売れた」と数えるのは6条件をすべて満たす時だけ。**
    観測あり／`observed_at` あり／観測が判断より後／`price_basis='SOLD_MEDIAN'`／
    `sold_count_30d>=1`／`avg_days_to_sell` あり。
    足りなければ `ACTUAL_SALE_UNCONFIRMED`。**勝ちにも負けにもしない。**
21. **観測できなかった相場は `NO_OBSERVATION` と記録する。参考値で埋めない。**
22. **実績によるスコア補正は件数で強さを変える**（5件未満=補正なし／5〜19=0.3／20〜49=0.6／50以上=1.0）。
    補正幅は上下 **±10点**（`MAX_SCORE_ADJUSTMENT`）。少ない実績で点数を動かさない。
23. **データ信頼度の止め方は2種類あり、混同しない。**
    手数料が概算 → データ信頼度が下がり**そもそも90点に届かない**。
    同一商品未確認 → **90点近くても蓋をして** `WATCH` へ落とす（`confidenceCapped`）。
24. **旧料金で過去利益を再計算しない。** 変更は `fee_change_log` に記録するだけ。
    取引時点の `fee_version`（`f{E|V}-{ハッシュ8桁}`）を保持する。
    Fee Version のハッシュには**金額に影響する14項目だけ**を含める。
25. **`hit_rate` は「売れた割合」ではない。** `AVG(probability_correct)`＝
    売却確率の予測が当たったかどうか。ここを取り違えると集計が全部おかしくなる。
26. テーブル名に注意：**`prediction_accuracy`**（`route_accuracy` ではない）／
    `shadow_market_snapshots` の列は **`role`**（`side` ではない）／
    手数料は **`venue_fee_profiles`** ／ `ownership_state` は **`supplier_products`** にある。
27. `calcFreshness(observedAt, opts)` の第2引数は **オブジェクト** `{freshHours, normalHours, now}`。位置引数ではない。
28. リポジトリのパスに日本語が含まれる。`new URL(...).pathname` はパーセントエンコードされるので、
    **URLオブジェクトをそのまま `fs` に渡す**。

- ルール版は Phase 1 が `v1-`、Phase 2 が `r1-`、Phase 3 が `r2-`、Phase 4 が `s1-`、
  Phase 5 が `a1-`、**Phase 6 が `g1-`**。**混ぜない。**
  （とくに **Phase 6 の `g1-` を Phase 5 の `a1-` と混ぜない**。門の判定と自動リサーチは別の責任範囲）

Phase 3.5 / 3.6（実市場検証）で追加。**ここが崩れると集めたデータが全部無意味になる。**

29. **「CSVだから規約上安全」は誤り。** ファイル形式は法的な立場の説明にならない。
    `SELF_OBSERVED`（自分で公開画面を見て記録）と `PROVIDED_BY_VENUE`（事業者から正式提供）を
    **同じ信頼度で扱わない**。`venues.csv_available` は「正式提供があるか」であって
    「取り込める形式か」ではない。**2026-08-20 時点で該当市場は0件。推測で1にしない。**
30. **「ページが消えた＝売れた」にしない。** 消えた・削除・404・非公開・取り下げは全部
    `REMOVED_UNKNOWN`。`CONFIRMED_SOLD` は**売り切れ表示を画面で実際に見たときだけ**。
    読み取れない言葉は `null` を返す（`parseListingState`）。**近い方に寄せない。**
    `SOLD_PROBABLY` のような「たぶん」の状態を作らない。
31. **売れた値段を推測しない。** 状態が `CONFIRMED_SOLD` でなければ成約価格を採用しない。
    確認できなければ `sold_price_known=0` ＝ 画面に「不明」。**出品価格でも0円でも代用しない。**
    一度判明した価格は後の観測で消せない（`COALESCE` / `MAX`）。
32. **`listing_observations` は追記専用。** 同じURLを観測するたび新しい行を積む。
    `UNIQUE(listing_key, observed_at)` ＋ `ON CONFLICT DO NOTHING` は二重取り込み防止であって、
    上書きの許可ではない。
33. **実績値には `real_market_data = 1` の行だけを使う。** テストデータ・記入見本を混ぜない。
    **画面に最初から入っている文字列は「取り込まれる可能性がある」前提で作る**
    （記入見本は `SAMPLE_MARKERS` で弾かれるようにしてある）。
34. **手数料は項目ごとに検証する。** 市場が決めている率（`VENUE_RATE_FIELDS`）だけ `VERIFIED` にできる。
    **送料・梱包費（`ITEM_COST_FIELDS`）は商品と発送方法で変わるので推定のまま据え置く。**
    率だけ確認済みは上限85点＝STRONG BUY(90点)に構造上届かない。
35. **測っていない数字を良く見せない。** 実市場0件は「的中率100%」ではなく **「測定不能」**。
    比較対象が無ければ「まだ比べられません」。**近そうな方を選んで書かない。**
36. **100件そろっても、それだけでは購入しない。必ず14項目の卒業条件で判定する。**
    **メルカリ100件が安定するまで次の市場を追加しない。**
37. **画面（`'use client'`）から読むファイルに `node:` の読み込みを混ぜない。**
    出品追跡の語彙は `lib/listing.ts`（外部依存ゼロ）から読む。`lib/observation.ts` はDB層を抱えている。
    `lib/conditions.ts` と `lib/normalize.ts` の関係と同じ。**テストでは見つからない。`npm run build` で見つかる。**

Phase 3.7（費用の分離・支払方法・検証ファネル）で追加。**ここも崩さない。**

38. **振込手数料を1商品ごとの販売費用として引かない。**
    メルカリの200円は「振込申請1回ごと」であって商品1件ごとではない。
    `fixed_fee`（1取引ごと）と `payout_fee_fixed`（申請1回ごと）を必ず分ける。
    **`fixed_fee` に200円を戻さない。** 戻すと1,000円の商品で費用を100倍見積もることになる。
39. **按分した振込費用（`payout_allocated_net_profit`）を判定に使わない。**
    `decision` と `route_score` は按分前の `expected_net_profit` で決める。
    按分は「参考」表示のみ。分母（`PAYOUT_ITEMS_PER_REQUEST`）を変えても判定が動いてはいけない。
40. **分からない支払手数料を0円として足さない。** `fee: null` は「不明」。
    不明のまま STRONG BUY にしない（`buy_payment_fee_known = 0`）。
41. **いちばん安い支払方法をシステムが勝手に選ばない。**
    使うのは設定 `DEFAULT_MERCARI_BUY_PAYMENT_METHOD`＝**実際に自分が使う支払方法**。
    `cheapest` / `lowest` / `best` のような選択関数を作らない（受け入れテストで検査している）。
42. **費用の確からしさ（`COST_CONFIDENCE`）を100点にしない。** 上限は `COST_CONFIDENCE_MAX = 95`。
    一部の費目が公式確認済みでも、全体を「完全確認済み」と呼ばない。
43. **決済手数料の「0円」を未確認のまま通さない（BUY側）。**
    支払方法で変わる費用なので、0でも確認を要求する（`NEVER_ASSUME_ZERO_FIELDS`）。
    そのためメルカリBUYの手数料は `ESTIMATED` のまま。**これを VERIFIED に上げない。**
44. **「100件登録した」を成果にしない。** 見るのは検証ファネル
    （観測 → 商品一致 → Route成立 → 答え合わせ対象 → STRONG BUY候補）と
    `MATCHABLE_RATE` / `ROUTE_GENERATION_RATE` / `SHADOW_ELIGIBLE_RATE`。
    **候補を増やすために判定を甘くしない。手数料の実額確認と仕入先を増やす方で増やす。**
45. **100件の前に10件（PILOT）。** 7項目すべて満たすまで100件へ進まない。
    この10件は「データを集めるため」ではなく「**どこを自動化するか決めるため**」に入れる。
    いちばん時間がかかった工程（`observation_work_log`）が次に自動化する場所。
46. **決済・購入・注文・発送を思わせる名前のテーブルを作らない。**
    支払方法ごとの手数料表は `venue_method_fee_rules`（`venue_payment_methods` ではない）。
    受け入れテストの「実行系テーブルが存在しない」検査を緩めるのではなく、名前の方を実態に合わせる。

Phase 3.8（まず10件のPILOT・実測で次に作るものを決める）で追加。**ここも崩さない。**

47. **落ちた理由に「その他」を作らない。** 8分類（`MODEL_NUMBER_MISSING` / `NO_SELL_MARKET` /
    `PRODUCT_MATCH_FAILED` / `MARKET_DATA_MISSING` / `FEE_UNVERIFIED` / `SHIPPING_UNKNOWN` /
    `INSUFFICIENT_MARGIN` / `LOW_DATA_CONFIDENCE`）のどれかに必ず入れる。
    「その他」があると面倒なものが全部そこへ入り、何を直せばいいか分からなくなる。
    **`INSUFFICIENT_MARGIN` が多いときに閾値を下げない。仕入先を増やす**（ルール13）。
48. **同一商品の答え合わせは3択だけ。「たぶん合っている」を作らない。**
    `CORRECT_MATCH` / `INCORRECT_MATCH` / `UNCERTAIN`。
    迷ったものは `UNCERTAIN` に置き、合格へ寄せない（迷うもの＝紛らわしいもの＝いちばん危ない）。
    **`INCORRECT_MATCH`（取り違え）は1件でも重大。** 見逃しは儲け損ねだけだが、
    取り違えは実際に金を失い在庫も残る。扱いを対称にしない。
49. **人の答えで機械の判定を上書きしない。** `tracked_listings.identity_key` はそのまま残し、
    人の答えは `identity_match_reviews` に別で持つ。上書きすると
    「機械が何回間違えたか」が数えられなくなり、精度を測れなくなる。
    **「違う商品だった」を選んだときは理由の記入を必須にする**（空欄では保存させない）。
50. **時間が長い工程＝次に自動化すべき工程、ではない。**
    「商品発見」「商品情報確認」は最も時間がかかるが、無断巡回しない決まりのため自動化できない。
    秒数だけで並べると、この2つが1位になって誤った結論が出る。
    必ず **① かかっている時間の順（事実）と ② 自動化して効く順（時間×自動化しやすさ）の
    両方**を出し、②を「次に作るもの」にする。`AUTOMATION_ROI` を
    「全部自動化して0時間」にしない。測れないときは数字を作らず「計算できません」と書く。
51. **記入見本・テスト用の行に書かれた作業秒数を「実測」に混ぜない。**
    出品は実市場データ判定で弾かれても、作業ログの秒数は残る。混ぜると誰も作業していないのに
    工程の順位が出て、それを根拠に開発方針を決めてしまう（ルール33・35違反）。
    出品1件ごとの記録（`batch_id` が `LISTING:`）は **その出品が `real_market_data = 1` のときだけ数える**。
    **除外した分は黙って引き算せず、「◯分は数えていません」と画面に書く。**
52. **データの点検で見つけたものを、勝手に消したり直したりしない。** 人に見せて、人が直す。
    自動で消すと「なぜ汚れたか」が分からなくなる。
53. **100件へ進む判定は「操作性7項目」と「安全条件7項目」の両方がそろって初めて合格。**
    片方だけで進まない。**まだ1件も確認していない状態を、黙って合格にしない**
    （`unmeasurable` を持ち、○にしない）。合格できないときに条件の方をゆるめない。
54. **受け入れテストのデータに実在の型番を使わない。** `P38TESTONLY001` のようなテスト専用の
    型番にして、後片付けを型番の文字列で確実に行う。実在の型番で消すと本物の相場を巻き添えにし、
    形が違うと消し残してデータベースを汚す（実際に起きた）。

Phase 3.9（正規Connectorと「購入ページを開く」）で追加。**ここも崩さない。**

55. **AIはURLを作らない。** 商品ページのリンクは、取得元データに**実在するURLだけ**を使う。
    `buildUrl` / `makeUrl` / `generateUrl` のような**URL組み立て関数を作らない**（テストで検査している）。
    URLの出どころは `CONNECTOR_API` / `CONNECTOR_FEED` / `OFFICIAL_CSV` / `HUMAN_ENTRY` の4つだけ。
    **`AI_GENERATED` という選択肢を作らない。**
    推測で組み立てると「実在するが別の商品」のページへ人を連れて行く。
    人は「AIが出したリンクだから合っている」と思うので、**いちばん気づきにくい取り違え**になる。
56. **商品ページの状態の既定は `UNKNOWN`。`ACTIVE` を既定にしない。**
    開けるのは `ACTIVE` のときだけ（Fail Closed）。`SOLD_PROBABLY` のような「たぶん」を作らない。
    市場と違うドメイン・転送/短縮URL・ログイン情報入りURL・`http(s)` 以外は問答無用で無効。
57. **`venue_connectors.state` を `CONNECTED` にしてよいのは4条件がそろったときだけ。**
    ①正式な利用条件を読んだ ②商用利用可を確認した ③出典URLと確認日を残した ④確認者を書いた。
    **「APIがある」は `CONNECTED` の理由にならない。** 既定は `NOT_RESEARCHED`。
    **2026-08-20 時点で `CONNECTED` は0件**（12の接続先を調査した結果。`19_Connector再調査_市場別スコア.md`）。
58. **「規約に禁止と書かれていない」を、やってよい根拠にしない。**
    **robots.txt は規約ではない。別サイトの規約を根拠にしない**
    （ブックオフの禁止条項としてよく引用されるものは `fan.bookoff.co.jp` という**別サイト**のもの）。
    確認できないものは「たぶん」ではなく **`UNKNOWN`**。
59. **廃止されたAPIを前提にコードを書かない。** 検索結果には古い情報が上位に残っている。
    使う前に**公式ドキュメントで現存を確認**する。
60. **押した記録（`page_open_events`）は凍結する。** 押した瞬間の価格と判断を写して固定し、
    あとから相場が動いても書き換えない。書き換えると
    「押したときAIは何と言っていたか」が消え、当たり外れを永久に測れなくなる。
61. **実際に買った記録（`real_trade_candidates`）を SHADOW の表に混ぜない。**
    `route_shadow_trades` に `actual_purchase_price` のような列を足さない。
    混ぜるとAIの予測成績に人の行動が紛れ込む。
62. **`AUTO_PURCHASE_CAPABLE` は9条件そろっても false を返す。**
    環境変数で有効にする経路も作らない（`lib/autopurchase.ts` に `process.env` を書かない）。
    **APIがあることは、自動購入してよい理由にならない。**
63. **出せない数字を画面に並べない。** `/buy` に利益額・ROI・Route Score を出していないのは、
    出品→Route変換層がまだ無く「この1件を買ったらいくらか」の形になっていないから。
    **それらしい数字で埋めず、出していない理由を画面に書く**（ルール35の具体例）。
64. **受け入れテストが自分の安全装置を落としたら、テストの方を直す。**
    ただし直してよいのは「**テストが事実を取り違えているとき**」だけで、理由をコメントに残す。
    歯止めを消して合格させるのは本末転倒（ルール46と対になる考え方）。

Phase 3.9b（市場ごとの許可を8項目に分ける）で追加。**ここも崩さない。**

65. **市場を「使える／使えない」の2択で判定しない。** 判定は3つ。
    `CONFIRMED_ALLOWED`（できると確認）／`CONFIRMED_PROHIBITED`（できないと確認）／
    **`CONFIRMATION_REQUIRED`（公式情報はある。当社用途に使えるかは書かれていない）**。
    **確認していないものを「禁止」と書かない。**
    実際にやった失敗：Yahoo!ショッピングの公式ヘルプに「原則 非商用」とあるのを見て
    「使えない」と結論づけたが、APIは現役で、同じ記事に「すべてを禁じるものではありません」とあり、
    商用相談の導線も公式にあった。**禁止と書かれていないから許可、の逆向きの間違い**である。
66. **同じ会社の別サービスを、同じ市場として扱わない。**
    Yahoo!ショッピング（商品検索API v3 が現役）と ヤフオク!（Web APIは2018年に提供終了）は
    **結論が正反対だった**。Amazonも SP-API／Creators API／Business API で結論が3つとも違う。
    `venue_connectors.venue_code` にはサービス単位のコードを入れ、`venue_ref` で市場に紐づける。
67. **許可は8項目に分けて持つ。** `api_exists` / `commercial_use_allowed` /
    `internal_business_use_allowed` / `price_analysis_allowed` / `data_storage_allowed` /
    `automated_collection_allowed` / `automated_purchase_allowed` / `automated_listing_allowed`。
    値は `YES` / `NO` / `UNKNOWN` の3つだけで、**既定は全部 `UNKNOWN`**。
    **「APIがある」は「使ってよい」ではない。1項目でも `UNKNOWN` が残るうちは本番接続しない。**
68. **`UNKNOWN` に点を付けない（0点）。** 「たぶん大丈夫」を点数に変えない。
    採点前と0点を見分けられるよう、`priority_score` に **`DEFAULT` を付けない**（DIGESTの原則）。
69. **技術点だけ高い市場を、合計点で選ばない。**
    「使ってよいか」の4観点（商用／社内利用／価格判断／保存）のどれかが0点なら、
    合計が何点でも候補から外す（Gate）。
    これが無いと Amazon Creators API（技術点は高いが、ライセンス13条(i)が
    「統合、分析、抽出もしくは再利用する目的」を名指しで禁止）を選んでしまう。
70. **公式ドキュメント内で数字が矛盾していたら、矛盾したまま全部記録し、一番厳しい値で設計する。**
    平均を取らない。片方を採用しない（Yahoo!のRate Limitは公式内に3つの数字がある）。
71. **お金がかかる決定と、既存事業に影響する決定は、AIが単独で実行しない。**
    **月額費用の発生・既存サービスが使えなくなる可能性は、必ず先に人へ提示する。**
    **2026-08-22 追記：費用の壁が消えても、それを進めてよい理由にしない。**
    大口出品は契約済みと判明して追加費用は無くなったが、代わりに規約本文を読んだ結果、
    AUP 4.3（原文「Amazonのウェブサイトから入手した情報またはデータを提供する外部の
    （Amazon以外の）データサービスの使用、提供、宣伝をしてはいけません。」）により
    **Keepa を併用できないことが確定**した。安くなったことは、着手してよい根拠ではない。
72. **「自動取得できない」を「市場として使えない」に読み替えない。**
    ヤフオク!はAPIが終了しているが、人が買うこと・売ることは普通にできる。
    `terms_status` を `BLOCKED` にすると仕入先・販売先ごと消えてしまう（実際にやりかけた）。

Phase 3.9c（規約の本文を実際に開いて読む・2026-08-22）で追加。**ここも崩さない。**

73. **調査記録に書いた出典URLも、時間が経つと死ぬ。引用するときに開いて確かめる。**
    `developer-docs.amazon.com/sp-api/docs/acceptable-use-policy` は転送先で
    「Page Not Found」になる。規約本文が実際に読めるのは
    `sellercentral.amazon.com/mws/static/policy?documentType=AUP&locale=ja_JP` の方。
    **ルール59は、APIだけでなく出典リンクにも当てはまる。**
74. **Amazon SP-API のカタログAPIは商品ページURLを返さない（2026-08-22 確定）。**
    ASINは返るので組み立てれば作れてしまうが、**それはルール55違反なのでやらない**。
    **SP-API単体では `/buy` の導線は作れない。** できない事実を、できることにしない。
75. **DPPにより、Amazonから取得したPII以外のデータは18か月以内に削除する義務がある。**
    「18か月保存してよい」ではなく「18か月以内に消さなければならない」。
    SP-APIを実装するなら、**削除処理は任意機能ではなく必須項目**。
76. **未確認だったことを調べて確定させたら、未確認欄から外し、結果を原文つきで残す。**
    調べたのに未確認へ置いたままにすると、同じことを何度も調べ直すことになる。
    **ただし「確定した＝安全になった」ではない。**
    実際に確定した2件（Keepa併用不可・商品ページURLが取れない）は
    どちらも当社に不利な内容だった。
    受け入れテストが「未確認欄に残っていること」を合格条件にしていたときは、
    **テストの方を直す**（ルール64。調べて確定させると不合格になるのは逆さま）。

Phase 3.9d（売れるかテスト・2026-08-22）で追加。**ここも崩さない。**

77. **Keepa は Amazon ではない。第三者のサービスである。**
    Keepa で見た数字を「市場から正式に提供されたデータ」と同じ箱に入れない。
    `sellability_checks.counts_as_real_market` は **0で固定**してあり、引数で切り替えられない。
    実市場データ100件に数え始めてよいのは、**ご本人が Keepa の規約を読んで
    商用利用と保存の可否を確認したあと**だけ。
78. **売れ筋順位の下落回数は販売数そのものではない。**
    1回の注文で2個売れても下落は1回のことがある。下落が起きない販売もある。
    そこから出した値は **必ず「推定」と呼ぶ**（変数名も `estimated*`、画面表示も「推定」）。
    確定した販売数として書いた瞬間に「正確性 > 安全性 > 利益」の順番が崩れる。
79. **「売れる」と「買ってよい」を混ぜない。**
    `/sellability` が出すのは「売れているか」だけ。
    利益・手数料・状態の判定は別。`SELLABILITY_SCOPE_NOTE` を画面から消さない。
80. **判定は4種類。「売れる／売れない」の2択に戻さない。**
    `CROWDED`（売れてはいるがライバルが多く自分に回りにくい）を消すと、
    回転しない在庫を掴むか、勝てる商品を捨てるかのどちらかになる。
    取り分の計算は **ライバル数 +1**（自分を数に入れる）。ここを +1 なしにしない。
81. **`SELLABILITY_THRESHOLDS` を黙って下げない。**
    下げれば候補は必ず増える。変えるときは理由・変えた人・日付をファイルに残す。
82. **「403で読めなかった」を、そのまま「読めない」と記録に固定しない。**
    一時「keepa.com は自動アクセスを403で拒否するので規約本文を読めていない」と書いていたが、
    **これは事実誤認だった（2026-08-25 訂正）。**
    403 になるのは**取得ツールの種類**による現象で、
    通常のHTTP取得なら `https://keepa.com/api-docs/` は **200 で読める**
    （`robots.txt` も `/api-docs/` を許可している）。
    さらに**利用条件（T&C, Version of July 28, 2026）は 2026-08-22 にブラウザで本文を読んでいる**。
    記録は `../事業Vault/AI Commerce OS/20_Keepa公式API調査.md`。
    → よって `KEEPA_API` の8項目は、条文を根拠に **`automated_purchase` `automated_listing` の
    2つが NO、残り6つが YES**（2(2) 商用可・3(4) 事業者専用・11(1) 取得と表示・11(2) 保存）。
    **教訓は2つ。①1つの道具で失敗しただけで「読めない」と結論づけない。**
    **②一度書いた「読めていない」を、次のセッションが事実として引き継いでしまう。**
    未到達は「未到達」であって「存在しない」ではない。訂正はためらわず上書きする。
    なお **keepa.com という「サイト」への自動巡回（ボット・スクレイパー）はいまも禁止**。
    触ってよいのは `api.keepa.com` だけ。ここは訂正されていない。
82b. **許可が取れたことと、本番で動かしてよいことは別。**
    Keepa は Gate（商用・社内・価格判断・保存）を通るようになったが、
    `mayStartAutoCollection()` は **`unknowns` が1件でも残っていれば false を返す**。
    8つの許可項目に収まらない問い（U1 正規化の線引き／U2 契約終了後の扱い ほか）が
    残っているうちは始めない。**Gate通過は「候補にしてよい」までの意味しかない。**
83. **調査の点数（Gate通過）と、いま進むかどうかは別。**
    2026-08-22、Amazon SP-API はご本人の判断で見送りになったが、
    77点・Gate通過という**調査結果はそのまま残してある**。
    見送りを理由に点数を下げると、再検討のときに「なぜ低いのか」が分からなくなる。
    また **Gate通過が0件になっても、2位を繰り上げない**（Gateの意味が消える）。

Phase 3.10（KEEPA_READ_ONLY・2026-08-25）で追加。**ここも崩さない。**

84. **件数の上限は設定ではなくコードで縛る。**
    `KEEPA_MAX_ASINS_PER_RUN = 1` は `lib/keepa/policy.ts` の定数で、
    **`policy.ts` には `process.env` を1つも書かない**（受け入れテストが検査している）。
    増やすにはこの数字を書き換えてコミットするしかない。
    環境変数で増やせるようにすると「設定ミスで100件取っていた」が起こりうる。
    段階は `KEEPA_STAGES`（1→5→20→100）で、いまは **S1（1件）**。
    次の段階へ進むのは、1件の結果を人が見てから。**AIが自分で進めない。**
85. **APIキーは「値を漏らさないよう気をつける」ではなく「値が出る経路を作らない」。**
    キー入りのURLは `fetchKeepaProducts` の中だけで組み立て、**関数の外へ一切返さない**。
    外へ出るのは `{endpoint, method, params}` だけで、`params` に `key` を入れない。
    保存する表に `request_url` 列を作らない。保存直前に `redactKeepaKey()` を通す。
    画面・ログ・コマンド出力に出すのは「設定済みかどうか」だけ（`keepaKeyStatus()`）。
    **先頭4文字だけ表示、もしない。**
86. **枠（Token）は「月にいくつ使えるか」ではなく「1分あたりいくつ戻るか」で管理する。**
    月の総量だけを見ると、短時間の大量取得で詰まる。
    `tokenHeadline()` は必ず「1分あたり」から書き始める（月間の総量を見出しにしない）。
    貯められる上限は `補充速度 × 60` で、**未使用ぶんは60分で失効する**。
87. **取得に失敗しても、枠を使った記録は必ず残す。**
    `runOneAsin` は、失敗しても `saveRawResponse` と `recordTokenUsage` を先に実行する。
    失敗を記録しないと「なぜ枠が減っているのか分からない」状態になる。
88. **同じ商品を24時間以内に取り直さない（`KEEPA_MIN_REFETCH_HOURS`）。**
    止めるのであって、**勝手に古い値を返さない**。連打で枠を溶かさないための歯止め。
89. **Keepa の価格は「その通貨の最小単位の整数」。円は100で割らない**（`KEEPA_JPY_DIVISOR = 1`）。
    ドルなら 1234 = $12.34 だが、円は 1234 = 1,234円。割ると1/100の値段になる。
    `-1` は「値なし」なので **null にする。0円にしない。** 評価（rating）は10倍で来る（45 = 星4.5）。
90. **日本のAmazon以外は保存しない。** `domainId` が 5 でなければ `saveNormalizedProduct` が拒否する。
    アメリカの相場で日本の仕入を判断しない。
91. **7日間の下落回数は Keepa に無い**（30 / 90 / 180 / 365 の4つだけ）。
    自分で数えれば作れるが、それは Keepa の数字ではなく自社計算なので、
    `provenance: 'NOT_AVAILABLE'` / `status: 'UNKNOWN'` / `rankDrops: null` のまま出す。
    **黙って自前計算で埋めない。**
92. **売れるかの判定式を Keepa 用に作り直さない。**
    `judgeFromKeepa` は、人が手入力するときと**同じ `judgeSellability()`** を呼ぶ。
    別の関数を作ると、同じ商品なのに出どころによって結論が変わる。
    観測時刻は `n.lastUpdateIso`（Keepa が最後に更新した時刻）であって、取得した時刻ではない。
93. **ASIN一致の拒否権は3つだけ**（バーコード違い／入数違い／ブランド違い）。
    **型番違い・商品名が似ていないは拒否権にしない**（日本語の表記ゆれで正しい候補まで落ちる）。
    1位と2位の差が `ASIN_MATCH_MIN_LEAD`（15点）未満なら**選ばない**。
    40点（`MIN_CANDIDATE`）未満は候補にもしない＝`MISMATCH`。
    材料が少なすぎるものを「人の確認へ」に流すと、確認待ちの山に埋もれて判断が甘くなる。
94. **`/keepa` の画面に取得ボタンを置かない。** 見せるだけ。
    ボタンにすると連打できる。取得はコマンド（`npm run keepa:one -- --asin=...`）からだけ行い、
    1回ごとに人が結果を読む。**U1・U2（正規化保存の可否／契約終了後の扱い）が未確認のあいだ、
    用途は `INTERNAL_VERIFICATION_ONLY` に固定する**（外部提供・再販売・本番自動運用をしない）。

95. **偽物のテストデータは、実物と同じ「形」で作る。**
    2026-08-25 の初回取得（B0978NB1VQ）で、在庫切れ割合の取り込み漏れが見つかった。
    Keepa の `outOfStockPercentage30 / 90` は1つの数ではなく
    **`stats.current` と同じ添字の配列**（0=Amazon本体 / 1=新品 / 2=中古）で来る。
    配列のまま `Number()` に通すと NaN になり、**値があるのに毎回「不明」**になっていた。
    受け入れテストの偽データが `outOfStockPercentage90: 4` という
    **実物と違う形**だったため、267項目のテストを全部通っていた。
    **値の正しさだけを検査しても、形が違えば意味がない。**
    → 新しい項目を足すときは、必ず一度**実物の生データを開いて形を確かめる**
    （生の応答は `keepa_raw_responses` に保存してあるので、枠を使わずに何度でも見られる）。
    **この抜けは「不明」に倒れるので一見安全に見えるが、安全ではない。**
    品切れが多い商品＝入り込む余地がある商品を、黙って見落とす方向に効いていた。
    **Fail Closed は「止まる」ことであって「静かに機会を捨てる」ことではない。**
96. **`-1` と `-2` を同じ「値なし」として扱ってよいが、`0` とは絶対に混ぜない。**
    Keepa は「値なし」を `-1`、「そもそも取得していない」を `-2` で返す
    （`offers` を付けていないときの `offerCountFBA` など）。
    どちらも `null`＝不明にする。**`totalOfferCount: 0`（本当に出品者0人）とは意味が違う。**

97. **「不明（UNKNOWN）だから安全」ではない。不明は必ず2種類に分ける。**
    - `DATA_NOT_AVAILABLE` … Keepa に値が無い。**市場データの問題**で、当社にできることは無い。
    - `PARSER_OR_SCHEMA_ERROR` … **値はあるのに当社が読めていない**。これは**当社のコードの不具合**。
    この2つを「不明」の一語に混ぜると、自分のバグが市場のせいに見える。
    実際、在庫切れ割合の取り込み漏れ（ルール95）は「不明」に化けて267項目のテストを全部通っていた。
    - **人へ見せるのは従来どおり「不明」でよい**（`unknownFields`）。
      **内部の監査には理由を必ず残す**（`unknownDetails` / `parserErrors` / `schema`）。
      安全側に倒すことと、原因を隠すことは別である。
    - 応答の**形**が想定と違うときは `SCHEMA_MISMATCH` として `findAnomalies()` に出す。
      **「読めなかったので不明」で静かに通さない。**
    - 判定は `lib/keepa/schema.ts`。**このファイルは何も import しない**（画面へバンドルできる＝ルール37）。
      `-1` `-2` `null` `項目なし` `空文字` `空配列` → データが無い。
      **それ以外で値が入っているのに読めていないものは、全部こちらの不具合とみなす。**
    - **鮮度の線（`KEEPA_FRESHNESS_MAX_DAYS = 30`）を、目の前のデータが古いからといって動かさない。**
      「今回だけ35日にすれば判定できる」は、判定できたことにしているだけである。

Phase 3.11（ASINの出どころ管理・2026-08-25）で追加。**ここも崩さない。**

98. **ルール55を2つに割る。危ないのは「URLを組み立てること」ではなく「材料が確かめられていないこと」。**
    ご本人の言葉：「以前の『AIにURLを作らせない』ルールと、『正式に取得したASINからAmazon公式形式の
    URLを決定論的に生成』は分けて扱ってください。」
    - **①AIが商品ページのURLを推測で作る → いまも禁止**（`AI_GENERATED` という選択肢を作らない）。
    - **②実在が確かめられたASINから公式形式（`https://www.amazon.co.jp/dp/{ASIN}`）で組み立てる → 別の話。**
      記録する出どころは `AMAZON_OFFICIAL_ASIN_PATTERN` で、**AI生成URLとして扱わない**。
    組み立ててよいのは **`lib/keepa/asinsource.ts` 1ファイルだけ**（他のファイルに `amazon.co.jp/dp` を書かない）。
    そこには**7つの検査**があり、**全部通らなければURLを作らない**（`resolveAmazonProductUrl`）。
    ルール74（SP-APIはURLを返さないので組み立てない）は**この改訂で置き換わった**。
    組み立て自体は禁止ではなく、**出どころが `KEEPA_API` などで確信度が `VERIFIED_EXISTS` のときだけ**行う。
99. **ASINには必ず「出どころ」を付けて回す。**
    `asin_source` / `asin_verified_at` / `asin_confidence` / `asin_verification_method_ja` の4点セット。
    使ってよい出どころは4つ（`KEEPA_API` / `OFFICIAL_AMAZON_SOURCE` / `AUTHORIZED_DATA_FEED` / `HUMAN_INPUT`）。
    **禁止は3つ（`AI_GUESS` / `STRING_GENERATION` / `UNVERIFIED_SEARCH_RESULT`）。**
    禁止の出どころが付いたASINは、**通信する前に止まる**（枠も1つも使わない）。
    確信度は3段階（`VERIFIED_EXISTS` / `REPORTED` / `UNVERIFIED`）で、**「たぶん実在」を作らない**。
    **出どころはコマンドの引数で受け取らない。** `--source=KEEPA_API` と書けてしまうと、
    どんな文字列でも「Keepaが確認した」と名乗れる。`keepa-one.ts` は
    `lookupAsinProvenance()` で `keepa_asin_candidates` から引くだけで、引き直すときも許可リストを通す。
100. **`URL_VALID` と `PRODUCT_MATCH_CONFIRMED` を最後まで別の値として持つ。**
    ご本人の言葉：「URLが正しくても商品一致が低ければ購入候補にしません。」
    2つがそろって初めて `PURCHASE_URL_AVAILABLE = true`（`purchaseGate`）。
    **`runOneAsin` は `product_match_confirmed` に常に 0 を入れる。**
    機械が自分で「同じ商品だ」と決めない（ルール48の続き）。
101. **購入導線には、実際にOfferがあるChild ASINを使う。**
    `variation_role` は4種（`STANDALONE` / `CHILD` / `PARENT` / `UNKNOWN`）。
    **`PARENT` と `UNKNOWN` にはURLを作らない**（`PURCHASE_URL_AVAILABLE = false`）。
    親ASINのページはサイズ・色を選ぶ前の画面なので、そこへ人を送ると別の枝を買わせる事故になる。
    判定は `productType === 5` と `parentAsin` の有無で行い、**分からないものを `STANDALONE` に寄せない**。
102. **候補選定で大量の枠を使わない。使った枠は用途を分けて記録する。**
    ご本人の言葉：「候補探しのために何百・何千ASINもKeepaへ投げないでください。」
    Product Finder は **10 ＋（結果100件ごとに1）**で、`perPage` の最小が50なので **1回11**。
    `stats=1` を付けると **+30** になるので**付けない**。Best Sellers（50）は使わない。
    `keepa_token_usage.purpose` を `DISCOVERY` / `PRODUCT_FETCH` に分け、
    **1商品あたりの実費（現在6.5）を出せる形にしておく**。合算すると「1件1枠」に見えてしまう。

Phase 3.12（二段階取得・5件実測・2026-08-25）で追加。**ここも崩さない。**

103. **取得を2段階に分ける。深追い（Deep Scan）は自動で走らせない。**
     下見（Cheap Scan）は**1件1枠**で13項目。深追いは Buy Box・出品者一覧を足すため**約8枠**。
     `npm run keepa:five` は候補を**挙げるだけ**（最大2件）で、**枠を1つも使わずに止まる**。
     ご本人の言葉：「22. Deep Scan候補　最大2件まで選ぶ。**選ぶだけ。まだ取得しない。**」
     `KEEPA_AUTO_ADVANCE_STAGE` は false。**5件の次に20件へ自動で進まない。**
104. **枠は用途ごとに分けて保存する。合計は「足し算」ではなく実消費から出す。**
     `DISCOVERY_TOKENS`（候補探し・分類の一覧）／`BASE_SCAN_TOKENS`（下見）／
     `DEEP_SCAN_TOKENS`（深追い）／`TOTAL_TOKENS`。
     **振り分けられなかった枠は必ず0**（0でなければ記録の穴）。
     2026-08-25 の5件実測は **12 / 5 / 0 / 17**、1件あたりの実費 **3.4枠**。
     ご本人の言葉：「目的は、**Tokenを多く使った商品ほど良い分析になる** という雑な設計を防ぐことです。」
     → 21項目めの「枠あたりの材料数」は**取り方の効率だけ**を見る数字で、商品の点数ではない。
     この但し書きを出力から消さない。
105. **「推定の月間販売数」という呼び方をしない。**
     ご本人の言葉：「30日Rank Drops 13回 等から『月12個売れている』と**単一の実数として扱わないでください**。」
     表示は **推定需要シグナル（月あたり相当）** と **推定自己販売機会（月あたり相当・暫定モデル）**。
     `Rank Drops ≠ Actual Sales` の文面は **`lib/sellability.ts` の1か所**から全画面へ配る
     （画面ごとに書き写すと、直したつもりの場所だけ直って他が古いまま残る）。
     **列名を慎重にしても、但し書きを消せば読む人は結局「販売数」として読む**
     （実際に `/keepa` と `/sellability` の2画面で消してしまい、受け入れテストが拾った）。
106. **商品ごとの「形」を毎回記録する（`keepa_field_shapes`）。値だけ見て通さない。**
     26パス×商品ぶんを保存し、`UNIQUE(run_id, asin, path)` ＋ `ON CONFLICT DO NOTHING` で上書きしない。
     2026-08-25 の5件で**3パスが商品ごとに違った**
     （`availabilityAmazonDelay` は1件だけ**項目そのものが無い**／`model` `partNumber` は null と文字列が混在）。
     配列の長さも一定ではない（`csv` `stats.*` は36で固定だが、`categories` は 6/4/2/1）。
     **固定長・項目が必ずある、を前提に書かない。**
107. **★2026-08-25 実測で判明した2件。→ Phase 3.13（同日）で両方とも修正済み。**
     - **`imagesCSV` はもう返ってこない。** いまは `images` という別名で、文字列ではなく
       オブジェクトの配列（`{l, lH, lW, m, mH, mW, variant}`）。5件すべてで「項目なし」だった。
       **ルール95と同じ型の見落とし。「不明」に倒れるので一見安全に見えるが安全ではない。**
       → **修正済み**（`lib/keepa/images.ts` を新設。保存済み7件すべてで画像が読めるようになった）。ルール109へ。
     - **Keepa は `monthlySold` を返している。** 当社の推定と**桁が違う**
       （ポケモンごいた 推定32.7 ↔ Keepa値200／チェキ 推定44 ↔ Keepa値5,000）。
       チェキの `CROWDED` 判定は、`monthlySold` を置くと結論が反対になる。
       **ただし5件中2件にしか無いので、これ1本に置き換えることもできない。**
       → **修正済み。** 呼び方をルール110で正し、食い違いは `DEMAND_SIGNAL_CONFLICT` として
       残す形にした（ルール112）。**判定式は変えていない。**
       **推定とKeepa値は別の欄で並べる。片方に寄せない。閾値・モデルはAIが勝手に動かさない**（ルール81）。

Phase 3.13（画像の読み取り・需要指標の食い違い・2026-08-25）で追加。**ここも崩さない。**

109. **旧フィールド名を黙ったまま「予備」として残さない。現在仕様を先に、旧名は後ろに。**
     ご本人の言葉：「旧フィールド名を黙ってFallbackとして残さず、現在仕様 ↓ 必要ならLegacy互換 の順にしてください。」
     公式仕様書（`keepa.com/api-docs/product-object.html`）の本文に **`imagesCSV` は1回も出てこない**。
     現在は `images`（`{l, lH, lW, m, mH, mW, variant}` の配列）で、画像URLは
     `https://m.media-amazon.com/images/I/` ＋ ファイル名で組み立てる。
     読み取りは **`lib/keepa/images.ts` の1ファイルだけ**（依存ゼロ＝画面へバンドルできる・ルール37）。
     旧名から読めたときは `imageLegacyFieldUsed = true` を立てて**必ず見えるようにする**。
     **黙って旧名で拾うと、仕様が変わったこと自体が誰にも伝わらない。**
     - **画像が無いことを1種類にまとめない。** ルール97の具体例。
       `IMAGE_DATA_NOT_AVAILABLE`（Keepa にも本当に画像が無い＝市場データの問題）と
       `IMAGE_PARSER_ERROR`（**Keepa にはあるのに当社が読めていない＝当社のバグ**）を
       **絶対に混ぜない**。混ぜると自分のバグが市場のせいに見える。
110. **外部サービスの「販売数」系フィールドを、実際の注文データと突き合わせない限り「実測」と呼ばない。**
     ご本人の言葉：「`ACTUAL_MONTHLY_SALES` / 実測販売数 とは呼ばないでください。
     正式名称を公式フィールド・公式説明に合わせて確認してください。」
     → 公式本文を読んだ結果、**「推定」でもなかった**。`monthlySold` の公式説明は
     **"It is not an estimate."**／値は **"10+" "100+" のような区切りの下限（"at least"）**／
     **"Most ASINs do not have this value set."**。
     よって当社の呼び方は **`keepaMonthlySoldAtLeast`（画面表示＝
     「Keepaの月間購入回数（「◯個以上」の区分値）」）** に統一した。
     `ACTUAL_MONTHLY_SALES` / 「実測販売数」の2語は **`FORBIDDEN_SALES_WORDS` として
     受け入れテストが機械的に見張る**（`lib/` `app/` `scripts/` を走査。
     **走査前に必ずコメントを除去する**＝ルール64）。
     **ご本人が挙げた `KEEPA_MONTHLY_SALES_ESTIMATE` も採用していない**（公式が「推定ではない」と
     書いているため。ご本人の「または公式名称に対応した名前」という許可の範囲で判断した）。
111. **需要の材料を1つの数字に統合しない。数字には必ず出どころを付ける。**
     ご本人の言葉：「この3つを一つの数字に統合しないでください。」
     `DEMAND_EVIDENCE`（`lib/keepa/demand.ts`・依存ゼロ）に **7つの材料を別々のまま**持つ。
     `RANK_DROPS_30D`(KEEPA) / `KEEPA_MONTHLY_SOLD_AT_LEAST`(KEEPA) /
     `INTERNAL_DEMAND_SIGNAL`(INTERNAL_CALCULATION) /
     `ESTIMATED_EQUAL_SHARE_OPPORTUNITY`(INTERNAL_CALCULATION) /
     `SELLER_COUNT`(KEEPA) / `AMAZON_RETAIL`(KEEPA) / `DATA_FRESHNESS`(KEEPA)。
     **表示と記録だけ。判定には使わない。**
     取り分は **「自分が月◯個売れる」と書かない**（`ESTIMATED_EQUAL_SHARE_OPPORTUNITY`＝暫定モデル）。
     **Keepa値が無い商品を自動的に低評価にしない**（`UNKNOWN` のまま他の材料で見る。
     `keepaAbsenceDowngradesScore()` は常に false）。
112. **需要指標が食い違ったら、どちらかへ寄せずに `CONFLICT` として残す。**
     ご本人の言葉：「勝手にどちらかを採用しないこと。」
     当社の推定と Keepa 値が **3倍以上**ずれたら `DEMAND_SIGNAL_CONFLICT`。画面には
     **「需要指標が食い違っています。追加検証が必要です」**と出す。
     どちらか片方が無いときは `CANNOT_COMPARE` で、**これは「食い違いが無い」という意味ではない**
     （材料が足りないだけ。ルール97と同じ考え方）。
     倍率（`KEEPA_ESTIMATE / RANK_DROPS_ESTIMATE`）は **分析専用**。
     **`lib/sellability.ts` は `demand.ts` を1行も import しない**（受け入れテストが検査）。
     こうしておかないと、食い違いや倍率が仕入判定へ染み出す。
     - **既存4判定は変えていない。** 実際の定数名は
       **`SELLS` / `CROWDED` / `DOES_NOT_SELL` / `UNKNOWN`**
       （ご本人の指示文にある `TOO_COMPETITIVE` / `NOT_SELLING` は同じものを指す呼び名。
       **コード側を指示文に合わせて改名しない**＝「まだ変更しない」の指示に従う）。
     - `CALIBRATED_SELLABILITY_SCORE` は **予定として文章で持つだけ。まだ実装しない。**
113. **AI Commerce OS のコミットに、別プロジェクトの変更を混ぜない。**
     ご本人の言葉：「今後は commit 前に、`git diff --cached --name-only` 等で対象ファイル一覧を確認し、
     AI Commerce OS以外が1件でも含まれたら停止してください。」「他プロジェクトの現在状態は勝手に変更しないこと。」
     このリポジトリは**独立した多数のプロジェクトを束ねた作業場**なので、
     `git add -A` / `git add .` は**絶対に使わない**（ルート直下に未追跡の巨大ファイルも多数ある）。
     - 手順は **①ファイルを名指しで `git add` → ②`git diff --cached --name-only` を実行して目で確認
       → ③`ai-commerce-os/` と `事業Vault/AI Commerce OS/` 以外が1件でもあれば、
       その場で止まって人に報告**（`git restore --staged` で外すのも人の判断を待つ）。
     - 実際に、別プロジェクトの変更を巻き込んでコミットする事故が起きている。
       **消えたわけではないが、他人の作業中の状態を勝手に確定させた**ことに変わりはない。
Phase 3.14（20件実測・需要指標がどのくらい使えるかを数える・2026-08-25）で追加。**ここも崩さない。**

114. **20件を取った目的は「利益商品探し」ではない。指標の効きを測るための標本である。**
     ご本人の指示（原文）：「**目的は利益商品探しではありません。**
     『どの需要指標が、どのカテゴリで、どの程度使えるのか』を実データで確認すること。」
     → だから `scripts/keepa-study.ts` は **儲かる順に並べない**。並べるのは
     **「その指標が何件で埋まったか」**だけ（`lib/keepa/coverage.ts`）。
     **候補指標TOP3は「採用」ではない**（`adopted` は常に `false`）。
     どれを使うかを決めるのは人であって、この仕組みではない。
115. **「値が無い」を 0 で埋めない。「0」と「不明」を必ず別に置く。**
     - 順位下落回数の **0回は「取得できた」に数える**（1度も下がらなかった、という**中身のある観測結果**）。
       ここを欠測に混ぜると、売れていない商品がデータの無い商品に化ける。
     - 割合の分母が0のときは **`null`（＝「—」表示）**。**0%と書かない**（`pct()`）。
       測っていないものを、測って0だったように見せない（ルール35の具体例）。
     - 在庫切れ割合の **「0%」と「不明」を同じ欄に入れない**。
       2026-08-25 の20件では**不明0件**で、ルール95の配列読み修正が効いていることを確認した。
116. **「◯個以上」を実数として書かない。**
     ご本人の指示（原文）：「✕ 月5000個売れている ／ ○ 月5000個以上の区分に該当」。
     表示文は **`formatBucketJa()` の1か所**で作る（画面ごとに書くと、いつか誰かが「以上」を落とす）。
     計算に使うときも**下限**として扱う。
     - 食い違いは4段階（`NO_CONFLICT` / `MILD_CONFLICT` / `STRONG_CONFLICT` / `NOT_COMPARABLE`）。
       **STRONGの線は従来と同じ3倍**なので、旧3種の判定は壊れていない。
       **閾値はまだ仕入判定に使わない**（`DEMAND_CONFLICT_LEVEL_USED_IN_BUY_DECISION = false`）。
     - **「比べられない」を「食い違いが無い」に足さない。** 材料が片方無いだけである。
     - **2026-08-25 の20件で、比べられた7件が7件とも「強い食い違い」で、向きも全件同じだった**
       （当社の推定が Keepa の区分値より小さく出る）。
       **これは「当社が間違っている」ことの証明ではない。** 成約データが無い以上、
       どちらが正しいかを決めてはいけない（`CALIBRATED_SELLABILITY_SCORE` を実装しない理由）。
117. **カテゴリの偏りは、偏りとして報告する。当てるために叩き直さない**（ルール108の続き）。
     2026-08-25 の20件は **本10件（半分）／おもちゃ4／ゲーム2／ビューティー・PCソフト・家電・洋書 各1**。
     指示にあった「日用品」「ホビー」「PC周辺」は**0件**だった。
     候補探しは **1回だけ**（`perPage=300` ＝ 14枠。上限15の内側で、**ぎりぎりに置かない**）。
     返った候補から**等間隔**で13件を選び、**すでに保存済みのASINは選ぶ前に外す**
     （24時間ルールで枠は使わないが、外さないと「新しい13件」にならない）。
     **100件でカテゴリ差を見たいなら、売り場ごとに件数を割り当てて探す必要がある。**
     いまの探し方のまま件数だけ増やしても、偏った標本が5倍になるだけである。
     - **20件のGateは5つ全部が0のときだけ通る**（読み取り不具合／実在しないASIN／
       日本以外のAmazon／枠の見積と実消費のズレ／カギの漏れ）。
       **通っても100件へ自動で進まない**（`KEEPA_AUTO_ADVANCE_TO_HUNDRED = false`）。
       進むには `KEEPA_CURRENT_STAGE` を人が書き換える。

118. **段階S4（100件）へ進めたのは人であって、仕組みではない。**
     2026-08-25、20件の結果（5条件すべて0）をご本人が読み、**「100件へ進むこと自体は許可します」**
     と判断された。**ただし同じ文に、外してはいけない条件が付いている。**
     **「現在の候補取得方法のまま、単純にあと80件増やすことは禁止とします。」**
     偏った標本を5倍にするだけの取り方に戻したら、**この許可は無効になる。**
     `KEEPA_AUTO_ADVANCE_TO_HUNDRED` は false のままにしてある。
119. **売り場ごとに枠の上限を持たせ、足りなければ「揃わなかった」と書く。**
     ご本人の指示（原文）：「カテゴリを揃えるために大量Tokenを浪費してはいけません。」
     「**無理に100件を見栄え良く揃えないこと。**」
     区分ごとに試行回数と枠の上限（`MAX_DISCOVERY_ATTEMPTS` / `MAX_DISCOVERY_TOKENS`）を持ち、
     届かなければ **`TARGET_NOT_REACHED`** として終える。**条件を緩めて数だけ合わせない。**
     2026-08-25 の100件では「その他型番商品」が **0/2 で TARGET_NOT_REACHED**。
     これは失敗ではなく、**正直に止まった記録**である。
120. **区切りで止まったら、必ず原因を調べてから進める。「実害が無さそう」で黙って通さない。**
     2026-08-25 の100件取得は、**設計どおり2回止まった**。どちらも枠を1つも使わず、
     保存済みの生データだけで原因を確かめた（ルール：修正の検算は `keepa:audit` 側で行う）。
     - **40件の区切り**：`eanList`（配列36件／null 2件）と `upcList`（null 36件／配列2件）。
       原因はバーコードの規格が地域で違うことで、**Keepaの異常でも当社の不具合でもない。**
       `schema.ts` は `expected: ['array','null']` と宣言済みで、読み取り不具合は0件だった。
     - **70件の区切り**：手数料まわり3か所。
       ①68件のうち**手数料が付いてこなかったのは1件だけ**（データ無しであって不具合ではない）。
       **不明な手数料を0円として足していない**（0円で埋めると利益が過大に出る＝ルール40）。
       ②**`referralFeePercentage`（新）と `referralFeePercent`（旧）は別の数字だった。**
       68件中67件で値が違い、新は 15.41 / 15.4 / 15.43 のような小数、旧は一律 15。
       **当社が読んでいるのは新しい方だけで、これが正しい。**
       ★**旧名の方が「きれいな数字」に見えるからと乗り換えてはいけない。**
         乗り換えると、1件あたり0.4%ぶん手数料を少なく見積もることになる。
121. **100件の目的は、利益商品を100件見つけることではない。**
     ご本人の指示（原文）：測るのは Schema安定性／データCoverage／需要指標Coverage／
     競合Coverage／カテゴリ差／Conflict率／Freshness／Token効率 の8つである。
     **「20件での暫定TOP3を固定採用しないこと。」**
     - **2026-08-25 の100件の結果**：価格99% ／ 順位下落30日100% ／ 出品者数99% ／
       在庫切れ90日100% ／ **Keepa区分値67%**（20件時点の35%から上がった）／
       Amazon本体100% ／ 手数料98% ／ 画像100%。
     - **売り場は10種に散った**（おもちゃ16／家電＆カメラ16／本13／ゲーム11／ビューティー10／
       PCソフト10／ミュージック10／ドラッグストア10／DVD3／洋書1）。
       **本が半分を占める偏りは解消したが、DVD3件・洋書1件のように少ない区分がある。**
       **サンプルが少ない売り場について断定表現を書かない**（指示21の原文）。
     - **食い違いは20件のときより広がった。比べられた67件が67件とも同じ向き**
       （当社の推定が Keepa の区分値の下限より小さい）で、逆向きは0件。
       **これだけでは、どちらが正しいかは決まらない**（ルール116の続き）。
       既存の食い違い判定は書き換えていないし、4段階の強度も仕入判定には使っていない。
122. **Keepaの区分値が「無い」ことを減点にしない。**
     ご本人の指示（原文）：Coverageが低いので「**ある場合は強い追加Evidence**」、
     「**無い場合は減点しないこと。**」100件で67%まで上がったが、扱いは変えていない。
     また、順位下落の強度（LOW/MEDIUM/HIGH/VERY_HIGH）や `RECENT_DEMAND_RATIO` などは
     **`SOURCE = INTERNAL_CALCULATION` であり、Keepaの値として表示しない**（指示10の原文）。
123. **Deep Scan と 判定モデルv0 は「作るところまで」。実行と本番適用はしない。**
     ご本人の指示（原文）：「まだDeep Scanは禁止」「ただしまだ実行しません」
     「**まだ本番適用しません**」「実成約データがまだないので、どちらが優秀かは決定しないこと。」
     - 2026-08-25 の100件で、Deep Scan候補の6条件をすべて満たした商品は **0件**
       （全件が「利益の出る仕入ルートがあるか＝不明」）。**使った枠は0。**
     - 判定モデルv0は既存モデルと**並べて比較しただけ**で、100件中9件（9%）判定が変わった。
       **v0は本番で使っていない。どちらが優秀かも決めていない。**
     - **100件が集まっても、購入・出品・決済・発送には進まない**（指示26の原文）。

108. **候補探しの回数を増やして「ジャンルを散らす」を成立させない。**
     ご本人の言葉：「候補探しのために何百・何千ASINもKeepaへ投げないでください。」
     条件は5ジャンル全部（本 / 家電＆カメラ / ゲーム / ビューティー / おもちゃ）へ投げているが、
     **候補探しは1回だけ**で、返った50件から**等間隔**に5件を選ぶ（点数付けをしない）。
     2026-08-25 の結果は 本3 / おもちゃ1 / 家電1 に偏り、**ゲームとビューティーは当たらなかった。**
     **偏りは偏りとして報告する。当てるために叩き直さない。**
     売り場は**番号ではなく名前のキーワードで探す**（catId を直書きしない）。見つからないジャンルは
     **推測で番号を作らず、黙って外す**。

Phase 4a（仕入価格 → Amazon販売のRoute検証・2026-08-25）で追加。**ここも崩さない。**

124. **Keepa単体で「買うべき」と判定しない。** Keepa は**Amazon販売側のデータ**として固定する。
     仕入価格が入って初めて BUY / WATCH / SKIP を出せる。
     ご本人の言葉：「**Keepa単体で、『買うべき』とは判定しません。**」
125. **仕入先データの分からない項目はNULL。推測で埋めない。**
     16項目のうち埋まらないものは空欄のまま持ち、**0円で代用しない**（ルール115の続き）。
     仕入先URLは**必須ではない**（URLが無くても分析はできる）。
     ただし **URLをAIが推測生成するのは引き続き禁止**（ルール55／98）。
126. **候補ASINが複数あるとき、勝手に1件へ決めない。**
     関門は3つだけ（`HIGH_CONFIDENCE` / `REVIEW_REQUIRED` / `REJECTED`）。
     複数候補は `REVIEW_REQUIRED` で人へ回す。**取り違え（FALSE MATCH）は今までどおり重大事故扱い**（ルール48）。
127. **想定販売価格は2本立て。`CALIBRATED` は列だけ作って、値は必ず空欄。**
     `RAW_EXPECTED_SELL_PRICE`（理論値）と `CONSERVATIVE_SELL_PRICE`（保守値）を別に持つ。
     **実成約データが1件も無いうちに補正値を作らない。**
     `store.ts` は `calibrated_sell_price` に常に `null` を入れる（保守値を流用しない）。
     **Buy Box が取れないときは無理に埋めず、確からしさを下げる。**
128. **Amazon側費用（Keepa由来）と当社側費用（送料・梱包・保管・返品損失）を混ぜない。**
     1つの合計額にまとめると、どちらの見積もりが外れたのかが永久に分からなくなる。
     当社側の6項目は `SUPPLIER_ROUTE_*` の設定から読む。
129. **「売れるから買う」は禁止。** 需要があっても、保守純利益と保守ROIの両方が閾値を超えなければ BUY にしない。
     判定 v0 は**透明なルールだけ**で作る（AIモデルを作らない）。
     **閾値は設定から変更できるが、変更するのは人。AIが動かさない**（ルール9・81）。
130. **需要指標の食い違い（`DEMAND_SIGNAL_CONFLICT`）と Amazon本体の存在で、即SKIPにしない。**
     ご本人の言葉：「**即SKIPにはしないでください／まだしない。**」
     WATCH に留めて材料を残す。**材料が足りないことと、買ってはいけないことは別。**
131. **脱落理由に「その他」を作らない**（ルール47と同じ）。10分類のどれかに必ず入れる。
     ファネルの件数は**必ず単調減少**（後の段が前の段より多くなったら集計の不具合）。
     **`INSUFFICIENT_MARGIN` が多いときに閾値を下げない。仕入先を増やす**（ルール13）。
     **KOMEHYOだけに固定しない。既存の Multi-Venue Engine を Amazon専用に作り替えない。**

Phase 5（AUTO RESEARCH ORCHESTRATOR・2026-08-25）で追加。**ここも崩さない。**

132. **司令塔（Orchestrator）に市場名を1つも書かない。**
     ご本人の言葉：「**OrchestratorはConnector非依存。特定市場コードを直接書かない。**」
     `KOMEHYO` / `MERCARI` / `KEEPA` のような文字列を `lib/phase5/orchestrator.ts` に書かない
     （受け入れテストが検査している）。市場は登録されたConnectorから読む。
     **市場名を直書きすると、市場が増えるたびに司令塔を書き換えることになり、
     「そのうち1社専用のシステム」に戻る。**
133. **取得手段は7段階で、スクレイピングは「禁止項目」ですらなく『存在しない』。**
     `OFFICIAL_API` / `BUSINESS_API` / `PARTNER_API` / `OFFICIAL_DATA_FEED` /
     `OFFICIAL_CSV` / `AUTHORIZED_INTEGRATION` / `MANUAL_FALLBACK` の7つだけ。
     **8番目を足さない。** 正式に使えない市場は無理に自動化せず、
     **`MANUAL_ONLY` / `WAITING_APPROVAL` / `UNKNOWN` と正直に表示する**（ご本人の指示§36）。
     ご本人の言葉：「**いきなり外部市場を勝手に検索しないでください。**」
     「**無断でWebを巡回しないこと。**」
134. **Phase 4a の「人が10件入れる」機能を削除しない。**
     ご本人の言葉：「**現在作ったPhase 4の10商品入力機能は削除しないでください。**」
     名前は `MANUAL_SUPPLIER_FALLBACK`。**主経路ではなくなったが、道は残す。**
     主経路が動かないときに人が前へ進める唯一の手段なので、消すと詰む。
135. **正式Connectorが無い市場のASINは、捨てずに `SUPPLIER_SEARCH_PENDING` として貯める。**
     ご本人の言葉：「**無断でWebを巡回しないこと。**」
     2026-08-25 時点で **47件が待機中**。これは「47件の儲かる商品を見つけた」ではなく
     「**47件を、探しに行く手段が無いので止めてある**」という意味である。**言い換えない。**
136. **目的関数は単純なROI最大ではない。**
     `期待純利益 × 売却確率 × 資金回転 × データ信頼度 × リスク調整`。
     ご本人の言葉：「**ROIだけ見ないでください。**」「1商品の最大利益より総利益。」
     見るのは `EXPECTED_NET_PROFIT` / `EXPECTED_DAYS_TO_SELL` / `CAPITAL_VELOCITY` /
     `EXPECTED_30D_RETURN` / `EXPECTED_PROFIT_PER_30D`。
     **売却確率は 7日/30日/90日 の3本で持ち、データが無ければ `UNKNOWN`。0%にしない。**
137. **全部をAIに投げない（Cheap Filter First）。**
     ご本人の言葉：「何万商品あっても全部をAIへ投げない。」
     「**既存の1〜5%絞り込み思想を維持してください。**」
     `RESEARCH_PRIORITY_SCORE` が低い候補には**AI費用もAPI枠も使わない**。
     ご本人の言葉：「AIの目標は『商品をたくさん調べる』ことではありません。
     **少ないAPIコスト・少ない人間作業で、利益商品を多く見つけることです。**」
138. **AI一致だけで買わない。画像だけで同一商品を確定しない。**
     ご本人の言葉：「**ただしAI一致だけで購入禁止。**」「**画像だけで商品一致確定は禁止。**」
     取り違え（`INCORRECT_MATCH`）は1件でも重大（ルール48）。
     **安い候補ほど一致の判定を厳しくする**（ご本人の指示§17）。
     異常に安いものは、まず 誤商品／状態違い／付属品違い／数量違い を疑う。
     **「安い＝チャンス」と読まない。**
139. **Demand First と Supply First の両方を持つ。片方に寄せない。**
     ご本人の言葉：「**Demand FirstとSupply Firstの両方を持ってください。**」
     売れているものから仕入先を探す道と、仕入先の在庫から売り先を探す道は、
     どちらか一方では取りこぼす。
140. **`WATCHING` は「まだ利益が出ない」を記録し続ける状態であって、失敗ではない。**
     `WATCHING` → `BUY_THRESHOLD_REACHED` → `PROFIT_OPPORTUNITY` → `EXPIRED`。
     「あと8,000円下がれば買える」を持ち続ける。
     **再評価は価格か在庫が変わったときだけ**（意味なく毎日AIを呼ばない）。
141. **出せない数字を朝の画面に並べない**（ルール63の続き）。
     ご本人の指示§2にある「調査商品 82,431件／利益候補 347件／BUY 42件」等は
     **画面の形の例であって、当社の実績ではない。**
     `/research` は**保存済みの記録から数えられる数字だけ**を出し、
     数えられないものは **0 ではなく「—」**（ルール115）。
     仕入価格が1件も無い以上、**予想利益は「0円」ではなく「材料が足りないので出しません」**。
142. **負けパターンも学習対象にする。** 当たった候補だけ振り返ると、
     「見送って正解だったもの」と「見送って損したもの」が区別できなくなる。
143. **Phase 5 でも、自動購入・自動出品・自動決済・自動発送は実装しない。**
     ご本人の言葉：「まだ、自動購入・自動出品・自動決済・自動発送 は実装しない。」
     Schedulerが自分で実行してよいのは **①正式Connectorからの取得 ②安い計算 ③記録** の3つだけ。
     ご本人の言葉：「**最優先KPIは、利益です。ただし、規約違反／無許可スクレイピング／
     危険な自動購入 によって利益を出すことは禁止です。**」
     「短期的な見かけの利益のために、**データの捏造／規約違反／判定基準緩和／リスク無視 をしないこと。**」

Phase 6（FIRST SUPPLIER CONNECTOR）で追加。**ここが崩れると規約違反になる。**

144. **順番を逆にする＝規約の確認 → 門を作る → 門が開いた市場だけ実装。**
     先に繋いでしまうと、後から「駄目でした」と言われても**取り消せない**。
     1回でも取得したデータは、消しても「取得した事実」は消えない。
     **`LEGAL_USAGE_GATE` が `BLOCKED` を返す市場には、取得コードそのものを書かない**
     （フラグOFFで止めるのではなく、`..._LIVE_FETCH_IMPLEMENTED = false` ＝実装が無い状態にする）。
     **登録の無い市場は自動で BLOCKED**（`isLiveFetchAllowed()` は未登録に false を返す＝Fail Closed）。
     `canFetchLive()` は **門・実装・全体スイッチの3つがそろって初めて true**。
145. **根拠の無い YES を書かない。原文引用＋出典URL＋確認日の3点セットが無い項目は YES と数えない。**
     `effectiveValue()` が、`evidence` の空な項目を**値がYESでもUNKNOWNへ落とす**。
     **要約した文を引用欄に書かない**（原文のまま置く。英語なら英語のまま。訳して意味を変えない）。
     公式資料が互いに矛盾していたら**どちらも消さずに両方載せ、厳しい側で判定する**（ルール70の続き）。
     過去の記録と今回の確認が食い違ったときも**過去を消さず、訂正を並べて残す**（ルール76の続き）。
146. **「区分」から金額を作らない。とくに「条件付き送料無料」を0円にしない。**
     「◯円以上で送料無料」「一部地域を除き無料」は、**この商品を1個買ったときに無料とは限らない**。
     0円と書いた瞬間、**利益を送料ぶん多く見積もる**ことになる。
     名前に「条件」「以上」「一部」「地域」「除く」「まで」が含まれるものは
     **金額を作らず「不明」へ倒す**（無料側へ寄せない＝Fail Closed）。
     同じ理由で **ポイントを現金として利益に足さない**（`POINTS_ADDED_TO_PROFIT = false`。別枠表示のみ）。

## ポート

3910（`amazon-ai-seller-factory` の 3900 と衝突させない）
